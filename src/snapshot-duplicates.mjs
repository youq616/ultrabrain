/** Node-compatible public path retained; browser and CLI share the exact same
 * inspected-handle boundary and duplicate algorithm, never a second implementation.
 */
export {DUPLICATE_METADATA_FIELDS,inspectMemorySnapshotDuplicates} from './personal-snapshot-contract.mjs';
