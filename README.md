# Personal-ranking transport-only branch

This isolated handoff tree contains only a bounded import workflow and transfer fragments. It is never merged into main. The reconstructed Git bundle must match the user's independently supplied SHA-256 ae7b98995c372dca17fbe79520d797a04153e29c78eb0ede6d7dd74079fedea5 before import. Import preserves the three original commits and runs no candidate application code. Main stays unchanged. This is transport, not an approval or CI pass; the receiver already reproduced a blocking PostgreSQL fixture error and will append repairs and request a new independent review.
