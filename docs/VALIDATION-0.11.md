# 0.11.0-alpha.1 验收范围

以具体 commit 对应的实际运行结果为准，不在此预填 CI 成功。

- 单元：明确采集同意、32 KiB 界限、合法 Unicode、独立模型开关、严格引用与类型、禁止模型决定状态/共享/分数、超时、Worker 身份模式和安全报告。
- PostgreSQL：原文/任务/回执原子入队，事件冲突/重复，所有权，模型输出事务，原文变化失效，旧租约/取消，失败重试次数、版本/profile 检查和生产插件接线。
- 真正的进程/协议：stdio/HTTP tools/list/call，以及实际 Worker 子进程通过锁定的原生模型 SDK 调用临时 loopback provider fixture。fixture 返回受控输出，不是实际 LLM。
- 管理台：保留已有真实 Chromium 操作，并验证原文入队、缺少模型时不假成功、任务取消。无浏览器权限时记录环境阻断，不绕过策略或以模拟 DOM 冒充。
- 运维：独立专用 profile 配置不变更摘要/向量身份，创建私有备份；新增 0013，不修改旧迁移；旧应用升级与数据库恢复覆盖任务和引用。

历史失败证据应保留：本地首次 claim SQL 产生重复 id 输出别名导致 ORDER BY 歧义，已通过限定表别名修复；旧工具数量断言从 8 明确更新为 12。测试不删除断言、不放宽授权门槛。系统 Chromium 对 loopback 的管理员策略阻断不通过更改浏览器策略解决，浏览器验收走仓库 CI 的隔离实例。
