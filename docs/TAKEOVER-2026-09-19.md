# 2026-09-19 接手审核与执行计划

接手基线为远程 main `77b4ecf649124fc169e4c838b5202d96211e933e`，应用合并为 `17bf51a2e998718665082a0245b5b49369ac6ec6`。通过 GitHub 核对后者七组 main 工作流均成功；文档提交自身没有新的应用 CI。已有个人记忆、MCP/HTTP、客户端队列、文本文件、恢复套件、服务部署、就绪和显式激活能力。个人 V1 仍未整体验收。

## 审核发现与本轮范围

三路代码审查覆盖个人 Store/文档/整理、客户端/自动采集/任务召回、部署/激活/恢复；实现代理另查浏览器管理台。此次审查不声称穷尽全部代码或证明没有其他缺陷。

| 优先级 | 可复现问题 | 处理与验收 |
| --- | --- | --- |
| P1，本轮修复 | `deliverCapture`、`deliverDocumentImport` 忽略授权回调的显式 `false`；持久化队列也未拒绝返回 Promise 的授权回调。既有内置 CLI 多使用抛错回调，公共 SDK 仍可传入这些返回值。 | 同步授权断言统一拒绝 `false`/Promise，保留成功返回 `undefined` 的旧调用方式；在等待锁、连接或身份校验后再次核对。拒绝时不得新存原文或新发请求。 |
| P1，本轮修复 | 后台整理在等待数据库准入期间，宿主模型配置可能被关闭或替换；捕获的旧模型仍可能收到原文。最终拒绝保存结果不足以阻止这次发送。 | 在实际模型调用前再次核对已启用的相同模型配置；默认模型网关加载后也复核配置与取消信号。增加可控制的等待屏障及真实 PostgreSQL 回归。 |
| P1，本轮修复 | 管理台保存/排队等待 Agent 注册时，用户可取消勾选或修改内容，但原请求仍被发送。 | 将授权绑定当前会话、选定表单和编辑版本；每次发出写请求前及重试时复查。回执丢失后再撤回授权，只停止重试，保留原事件和未确认状态。 |
| P1，下一产品阶段 | 首次 bootstrap 不创建管理台令牌；受管激活要求预先存在令牌，而现有公共创建路径会直接启动管理台。实例 UUID 与安装回执也需从多处收集。 | 增加不启动监听端口的首次配置入口，复用私有令牌校验与可信本地实例身份；串联现有部署/激活命令并验证可重复执行。 |
| P1，后续客户端修复 | 采集路径总以默认元数据注册 Agent，已有不同类型/能力/工作区的登记会被服务端版本控制拒绝，导致正常采集受阻。 | 先查找并保留已有登记；为已有自定义 Agent 的首次采集和队列重试增加真实协议测试。当前版本控制会拒绝覆盖，不把此问题描述为元数据已被静默改写。 |

本轮只实现前三项，保持应用变更可独立审核。现有数据库迁移、上游锁和独立 Windows qbrain 项目不变。队列中已确认交付的事件可正常清理；授权撤回不能撤销已发送的请求、已存的数据或已产生的模型费用。

## 后续开发顺序

1. **完成本轮授权修复。** 对固定候选运行客户端/浏览器/整理回归、必要 CI 和新的独立代理审查；保留初次复现与失败证据。仅在最终提交审查和测试通过后合并。
2. **完成首次使用闭环。** 处理令牌初始化、实例身份与回执指引、已有 Agent 采集兼容；以一个选定客户端验证安装、提交候选、管理台确认和任务召回。增加从没有预置管理台令牌开始的真实 Linux 集成，后续再做用户主机验收。
3. **完成客户端实际回合与记忆质量验收。** 逐项记录真实客户端引擎、工作区与身份隔离、撤回/重试、引用与错误纠正。没有凭据或用户安装环境时，可先完成测试与交接工具，不能把合成 Hook 或模型输出计作实际验收。
4. **扩展文件能力。** 先定义 PDF/图片/OCR 的大小限制、原始字节/页码引用、显式传输授权、归档传播及恢复合同，再实现对应导入和客户端流程。当前只支持首批 UTF-8 文本格式。

暂停新增企业功能与广泛运行中升级机制；它们不会解决首次个人使用流程的缺口。旧 PR #8 与主线冲突且部分功能已被后续主线覆盖，维持独立草稿，不直接合并。

## 验证记录的归属

接手基线本地 Node 运行：563 项中 558 通过、5 失败、0 跳过。失败属于既有服务/预检的普通账号门槛；当前容器 UID 为 0。保留原日志，不修改生产普通账号限制，也不把基线失败记成通过。仓库已有普通 Linux 账号 CI 可执行真实数据库、服务及浏览器回归。

独立部署审查 `/root/audit_operations` 执行 21 项运维测试和 2 项激活边界测试，全部通过；它们是离线夹具，不代表真实用户安装。本轮其他测试、固定候选、独立 reviewer 身份、CI 链接与最终结论在完成后另行归档，不能以此计划代替验收。
