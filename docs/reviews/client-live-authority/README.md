# 长连接授权阶段：实现自审与统一验收入口

## 基线

应用基线为 PR #19 `9ff749ad1ea790e71292a090af88507db72d33a0`，Source snapshot 工件 10584877665，ZIP SHA256 `8b72a83d2c0565087ddcc98021a54db9c747b61669fb7b61761e7842b0d9804d`。解包后按原执行位、历史跟踪日志与四个 gitlinks 重建的索引树精确等于 `e34d039767b7855f7277ecf4755d09f780aa23ee`。

已读取 main 当前完整提交 `491bf6fd702d8b88d30d65acb23b259a99242fcb`。本轮新分支以 #19 为父提交，但 PR 面向 main，作为 #15 已复用修复及 #16—#19 累计改动的统一审核入口，不再只审核最顶端的增量。不覆盖、关闭或强推任何原分支，不把旧 PR 的未完成批准自动转为已验收。

## 复现与修复

1. 旧 CLI 启动代理后不再提供 profile 复查断言；旧 runtime 的 `callAllowed` 与目录没有连接级授权。旧代理 tools/list 直接返回启动时缓存。已撤回的 profile 仍能驱动旧长连接。
2. 旧 `callAllowed` 在 await 身份期间仍保留原 args 引用，程序化调用方可以在已有 consent 检查之后更换嵌套内容和 event_id。
3. 下游工具取消信号只传递到最后一次工具调用，身份检查阶段不继承它。

先对原始 runtime 执行18项新测试，2通过、16失败；这是上述边界的不同用例，不是16个独立漏洞。测试用 Node loader 精确替换三个 SDK 导入为合成对象，其他源码未改写，没有将模拟 transport 当成真实SDK/数据库证明。完整首次输出在 `first-failure.log.gz`。

实现新增同步授权检查和不可变请求发送封装，CLI 为 mcp 提供遇到变化后锁定拒绝的 profile 断言；各 await 后及数据返回前复查。tools/list 使用新目录、请求级取消和初始集合交集，不再直接返回缓存。发送后的拒绝不会伪装成回滚，写结果仍按 unconfirmed 处理。原有服务端 scope/principal、SQL、迁移、pins、Agent 登记 CAS 和记忆审核状态机均未改变。

## 自审与已执行本地测试

实现助手分别检查了请求复制时点、回调返回 false/Promise/抛错、撤权锁定与恢复、原始错误脱敏、逐页目录取消、连接与请求信号组合、身份漂移拒绝、读取响应屏蔽及已提交写入不可撤回的合同。自审期间补充工具目录的请求级取消与集合交集；最终全量重新执行通过。

普通账号 Node v22.16.0：694/694，零失败、零跳过；系统 Python 3.13.5：683/683，退出码均0。相对 #19 新增38项 Node（20 runtime、12 profile、6同步授权），Python 未改。语法与 git diff --check 通过。完整日志哈希见 local-evidence.json；完整通过日志作为补充会话附件，不包含用户内容或真实凭据。

真实集成接入现有 Task-aware personal recall 工作流，以 npm 实际安装的本提交客户端包执行完整记忆生命周期和三处真实协议响应暂停。另加入 Linux/Windows 客户端合同矩阵。创建本记录时新候选 CI 尚未执行，不能把16失败的模拟复现或父版本绿灯当成新真实集成通过。

## 独立审核门槛

本文是实现助手的自审，不是独立 Agent 审查。实际独立 reviewer 必须审查新候选对 main 的完整累计差异及关联边界；请求其记录完整SHA、真实审查标识、文件行号、执行或仅查看的测试、明确结论。若有阻断发现，修正后复审新SHA；未披露的会话或测试明细不编造。CI/独立审查在创建此记录时均 PENDING，实际结果随统一PR更新。main 与用户运行服务保持不变，个人V1/实际Agent模型/用户部署未宣告完成。
