# 文档模块收尾：实现与自审记录

基线为远端实际读取候选 `2ed1e2a23715c8cbe6bf3b95225c9dd81116c96a`，完整树 `7e4aebd55dad12a801e32583493e1d4c78ba0ac4`。原本聊天中的「仅本地未提交」状态已经变化；本轮已读当前PR与提交，不重复应用旧补丁。2ed独立结果5749575327明确报告无重大问题，但不认证本轮增量。

## 完整模块而非单个函数

连接并统一验收元数据分页/状态过滤、明确导入、绑定原文读取、纯文本预览、原字节下载、分片排队、归档保留及异常回执原事件重放。本轮新增缺失的列表选择边界和UI状态过滤；生产改动只在app.js与index.html，没有新增后端、SQL、迁移、MCP、权限、依赖、上游锁或用户服务配置。

列表先整页验证再展示，禁止错误来源、正文混入、重复ID、错误过滤结果、非法元数据与异常游标；冻结传给卡片的公共标量字段，旧会话/来源/偏移/过滤响应不能覆盖当前页。切换筛选回到第一页，页面查询失败清除旧游标和导出权限但不清除独立草稿。

## 先复现再修复

在精确2ed应用脚本上执行新增43项模拟回归，7通过/36失败；修正后43/43通过。组合既有文档回执79项和记忆回执70项共192/192通过，0失败/取消/跳过。全部是实际页面JS在模拟DOM/HTTP下执行，不称为真实Chromium或PostgreSQL。首败原始日志摘录压缩入库，完整原始日志随交付包保留；原始日志/退出码哈希见local-evidence.json。

本轮本地最初环境由历史源码归档和增量恢复；app.js和index.html单独按远端文件blob核对，再从其上修改。原本地旧读取测试与远端存在数量差异，未用旧文件覆盖远端的新测试，也未把该混合树当作精确最终全量验证。最终整树通过CI的git archive来源重新取得后，才可对完整候选进行本地全量测试。创建本文时精确新候选全量CI、全量本地重跑、独立新SHA复审均PENDING。

## 实现者自审与真实集成门槛

自审检查整页原子拒绝、已知公共投影、不可变按钮选择、服务器过滤而非本地假筛选、游标恢复、错误不冒充空库、会话/来源/视图/偏移/无事件过滤变化，以及保留草稿和既有待确认事件。JS语法、Python AST及新增工作流检查完成。实现者复查不是独立reviewer。

新真实整模块夹具使用23个合成文档，检查20+3分页、21个使用中和2个归档过滤，再由真实UI执行一次导入/预览/下载/排队/归档，原文逐字节回环；用真实列表响应做五种故障注入，要求无部分卡片或导出。最终验证3事件、1文档、1片段、1stale零尝试任务，无无关草稿入库。既有19/11/10/8/12浏览器阶段、原控制台及n8n工作流保持。

独立审查请求需指定完整最终SHA及main累计边界，发现阻断后修正并再次审核。实际reviewer未公开的内部session/逐文件覆盖/运行明细不编造。远程主线保持不变，模块验收不是全套个人V1发布或用户生产部署，也不作零缺陷保证。
