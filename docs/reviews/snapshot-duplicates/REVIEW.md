# Snapshot duplicates: implementation review and independent-review handoff

Base: `8d373b38195f0134a2374cd3996811568d5621e9`, tree `41259f0550ce6bb401de965147d1e441bdf90fd6`. Source restored from the prior published archive; all original modes and four gitlinks reproduced this exact Git tree. Local reconstructed baseline history is not remote history. Publish on a new draft branch, preserve PR25/24/23/20 and main, and never deploy user services in this phase.

## Scope and manual review

Adds the `duplicates` operation to the existing offline CLI, path and byte APIs. All records are verified with the canonical snapshot contract before exact-content grouping. Equality uses actual well-formed content strings, not digest equality alone. No normalization, filters, semantic judgment, live lookup, model, merge or deletion. All states/projects/origins are retained; projected metadata and difference-field names support manual review only. The smallest ID labels a group but is never a keep recommendation. Output omits bodies/provenance/derivation values, remains private metadata, and never asserts identity, truth or safe automatic merging.

The implementer checked canonical-handle authentication before data access, whole-file fail-closed verification, immutable selection/byte copies, Map key safety, deterministic ordering, count partitions, all metadata differences, cross-project null handling, equivalent JSON object-key order versus array/type differences, 1000-record fan-in/fan-out and cancellation in grouping and projection. Forty deterministic mixed snapshots were compared against an independent O(n²) reference algorithm; this is an algorithm oracle, not an independent agent.

Self-review reproduced a pre-existing shared offline exception bug: snapshotFailure read error.code directly, so an exception accessor or revoked Proxy could escape sanitization. Two regression assertions failed before the guarded data-descriptor projection; after the fix the module's60 tests pass. The first complete suite then found the old static import-closure test assumed10 modules rather than11. Replaced that numeric-only check with an exact independently listed set of all11 modules; retained the existing approved-builtin and no-environment checks. No guard or test was removed. Exact first-failure excerpts and raw-log hashes are committed; full logs are in the delivery archive. The initial missing-module TDD failure is not a vulnerability finding.

## Executed local evidence

Ordinary uid1000, Node22.16.0: full suite2504/2504, zero failures/skips/cancellations. New60 tests are a subset, not additional to that total. Bun1.3.13 builds and npm pack succeeds. The extracted private tarball's CLI/path/byte APIs are copied into a directory without SDK and run under the existing network/subprocess/descriptor-write guard:10 new checks pass. The previous compiled offline regression also passes37 checks. Selected input bytes and modification times stay unchanged. This is an actual built tarball test with synthetic snapshots, not fresh dependency installation, PostgreSQL/model-quality evaluation or a customer-host install. No database was started for this offline module. Python and Windows are not claimed as local runs. See local-evidence.json for hashes and precise scope.

CI adds a separate Windows/Ubuntu test step without replacing any existing command. The existing task-context workflow runs the new offline package probe after real npm installation and uploads only its synthetic metadata report. No changes to applied migrations, upstream pins, server tool catalogs, n8n integration files or customer configuration. The earlier blocked n8n test upload is not retried or re-encoded by this phase.

## Independent review gate

Status: **PENDING / NOT APPROVED** at implementation commit creation. This document is the implementation assistant's self-review, not a separate agent approval. A new worktree, independent reference algorithm or green CI is not an independent agent. Record the actual reviewer session/run, full reviewed SHA, concrete file/line findings, actual tests and verdict on the PR; review the corrected SHA again after any blocking fix. Keep the PR draft and main unchanged while this gate is unmet.

Reviewer handoff: inspect the final PR head against the exact base above in a clean temporary checkout, read this document and docs/SNAPSHOT-DUPLICATES.md, independently inspect the whole-file trust boundary and same-text-versus-record-equivalence semantics, all projected fields, collision and Unicode behavior, count completeness, large-group event-loop cancellation, exception sanitization and import/compiled-package negative capabilities, then run the new test files and inspect final-SHA CI evidence; record findings with file/line, actual reviewer identity and full SHA, do not approve based solely on this self-report, do not merge/deploy or use user secrets, and leave blocking findings on the draft PR for correction.
