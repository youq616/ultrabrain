# 本地快照核验模块：实施与审核记录

基线：4d07e39dcfc2815901111c4ac0dd4934799d5be3，完整树75830bc325127108a09b1f7ab0851cee0d50ae54。实际下载该提交的CI源码工件10631810455，核验ZIP指纹和测试ref父提交；使用归档的全部文件（包括原已跟踪但被ignore的证据）、执行位及四个原gitlink重新建立索引，完整树一致。没有用旧677补丁覆盖最新代码。

本阶段实现了选择本地文件→明确读取同意→复用规范导出核验→单独对照同意→分页差异→明确逐条正文核对／无正文清单导出。只使用所选文件，不请求当前快照，不恢复或写入数据。生产变更限定共享快照合同追加的纯逻辑、新UI脚本、HTML接线及固定脚本资源；原app.js、原snapshot-ui.js、数据库/MCP/模型/权限/迁移与上游锁均不变。

首先添加四项模块合同测试，在缺少新模块的基线上4项失败（函数尚未实现，不是四个旧漏洞）；原始日志以first-missing-module.log.gz保留。实现后扩充非法编码、重复转义键、深度、两层体积限制、1000+1000记录完整并集、属性顺序、同版本派生变化、异步取消与原输入突变。页面测试执行实际三个脚本配合合成DOM，覆核选择/同意/会话/导航变化、第二文件失败整批拒绝、迟到失败、双击、纯文本显示、正文清除及无正文报告。

实现者自审再次核对文件内容没有进入HTTP/API，报告无正文或文件路径，原生读取异常不回显私密路径，操作不改变pending或草稿，并固定全部基线跨平台测试清单。真实浏览器集成追加在现有服务端快照验收之后、工件上传之前，不替代原任何阶段。自审为实现者检查，不是独立代理审查。

普通账号oai（uid1000）、umask022执行完整本地回归。首次完整Node1462/1462和Python683/683均退出0；之后将读取开始提示改为明确“本机核验”，避免沿用上一次成功消息，最终同样重新执行全量。最终退出码/时间/源码与日志摘要由local-evidence.json记录。新模块共新增62项Node（61项两个专项+1项原控制台资源边界），Python单测未改变。没有将准备写入或模拟DOM说成只读真实浏览器。

创建本记录时，新最终SHA的远程CI及独立Codex复审PENDING。旧4d07的8组CI成功和独立评论5758302756仅适用于基线。新浏览器定义14项，须实际读取日志/报告，检验无数据HTTP、仅一次明确报告下载和六表不变。任何独立阻断发现须修正，并对修正后的最终提交重新审查。main与用户运行服务保持不变，不以测试全绿或活动Completed代替独立结论。

详细用户合同与限制：docs/PERSONAL-SNAPSHOT-INSPECTOR.md。文件内的来源与所有者标志不是身份认证；差异不是删除推断、当前数据库状态或恢复计划。最终验收结果在PR #20补记，不回写历史失败为成功。
