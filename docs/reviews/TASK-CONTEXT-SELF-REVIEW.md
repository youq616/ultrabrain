# Task-context implementation self-review

This record is the implementation assistant's separate review pass, not an independent reviewer or a claim of zero defects. The final PR must carry the exact candidate SHA, current CI and an independent Codex written outcome. No review request, green unit suite or historical approval substitutes for that gate.

Scope: new client-task-context helper, actual packaged CLI and official-SDK runtime, profile/config generators, rollback preservation, task/Claude input contracts, last-mile identity/authorization/cancellation, output eligibility, existing native logging, real database and HTTP integration. No server schema, ranking algorithm, raw MCP tool contract, service configuration, upstream lock or capture/model permission was changed.

Checked the complete diff after implementation. Both new commands are disabled by default, task querying has its own authorization distinct from saving/model execution, automatic Claude reading requires an additional scope, and identity/workspace pins are mandatory. The original profile remains authoritative across stdin/connection waits. Task bytes and consent are snapshotted before awaited identity checks; authorization is synchronous and checked before starting requests and after responses. Revocation cannot retract an already sent query, but prevents output of its late response. Child events and non-prompt events never read task text. No transcript/file/tool getters are read. Malformed/oversized tasks are refused rather than shortened or turned into silent global recall.

Client-context eligibility/hash/budget checks remain in the response path. The new feature deliberately does NOT redefine the already available raw MCP context tool: its callers could explicitly send task hints before this phase. Documentation calls out this non-global gate, the distinction between read-only database behavior and server/operator logs, alpha package/source identity, duplicate legacy/new prompt Hooks, literal rather than semantic scoring, and no actual Claude model certification.

Executed evidence to be finalized in PR: 23 new Node contract tests, 9 new Python config tests; 19 real built-Node/stdin-fixture/stdio/read-only-HTTP/PostgreSQL checks. These include 105 newer entries versus an old task-relevant preference, unauthorized projects/states, synchronous stdin profile replacement, legacy Hook behavior, strict budget, identity mismatch, token revocation and unchanged full personal-memory/registry/job/document table fingerprints. Config tests plan/apply/rollback only synthetic files. The first all-Python outer command hit its 180-second runner timeout before finishing; it is NOT counted as a passing suite. A separate complete rerun is required and recorded separately. Full Node and completed Python results, package verification and CI outcome belong to the frozen candidate PR.

Local managed PostgreSQL uses the existing historical CI runtime with matching PostgreSQL/pgvector pins, not a newly compiled runtime or a user installation. CI bootstraps its own synthetic installation and installs this exact local tgz before exercising commands. No live systemd/Claude model, paid provider or user's machine was used by these task tests. Existing full, portability, native-host, recovery and service CI gates remain required.

## Final local verification before publication

Completed rerun: 520 JavaScript tests passed with zero failures/skips; 273 Python tests passed. The exact tgz was built and its 13 shipped files verified, unpacked into an isolated installed-package layout with the existing pinned SDK, and the unmodified task integration passed all 19 checks through that entrypoint. Existing built-client/proxy 19, capture/outbox 20, document-client 12 and database ranking 20 checks also passed. The CI-only npm install remains a separate pending acceptance gate, not claimed from local unpacking. A combined outer integration wrapper timed out without a completion marker; the subsequent individual completed runs, not the interrupted wrapper or diagnostic instrumented copy, are the accepted local evidence. Test data and reports are synthetic.
