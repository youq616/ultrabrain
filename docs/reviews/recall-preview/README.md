# 任务召回预览：开发与自审记录

## 固定基线与范围

基线 `485823df3956769da51560c69c5c9095bb21cf1b`（PR #20），原始树 `6cae3e24262fb29d9567ca1ef56ff752b16ae8b8`。从固定 #19 源码快照和上轮已交付增量补丁恢复，重建所有原执行位、历史证据与 vendor gitlinks 后核对完整树一致。其七组 PR 工作流已实际完成并成功；不据此认证本次新增代码。

本轮增量继续追加到统一 PR #20 开发分支，不改变 main、旧 PR 分支、迁移、数据库布局或 upstreams.lock.json。不重复开发初始化，也不扩张企业/模型权限。新增实际管理台只读任务预览、完整响应预算与内容指纹验证、失效/取消保护和明确导出范围。使用合同见 `docs/PERSONAL-RECALL-PREVIEW.md`。

## 实现后自审

检查了既有 console 的固定方法映射和认证、PersonalMemoryStore.context 的只读事务/主体/project/active/派生过滤/统一排名，以及浏览器的 fetch、表单、export、load、lock 和写入 pending 流程。预览只调用既有 context，不调用 register 或任意写方法。编辑器在此视图隐藏，结果卡片无写操作。

逐项复核提交前授权、Unicode 字节边界、等待与哈希验证后的条件/会话比较、旧请求与新请求竞态、锁定后清理、模型与任务未持久化声明、预算并非 token、dropped 仅窗口内计数、空结果不等于弃答、只读预览不等于所有客户端身份。

自审追加顶层回执字段白名单，避免将后端意外回显的 task/token 字段写入导出，并为该情况、哈希等待中的撤权、变化的来源、未确认写入阻止预览和成功空结果补充回归。未知回执失败是显式合同错误，不降级为成功空结果。哈希仅验证内容完整性，不认证语义或真实性。

## 已实际执行

普通账号 Node v22.16.0：**732/732**，零失败/取消/跳过；系统 Python 3.13.5：**683/683**，退出码均 0。Node 比基线 694 新增 **38** 个预览用例，Python 单测不变。新浏览器测试脚本不属于 unittest discover 的计数。语法与本轮增量 git diff --check 通过。全量 Node 最后重跑包含自审修正。

新增前最初32项特性测试全部失败，原因是功能尚未实现；这不是32个既有缺陷或漏洞。首个实现运行中17通过、15取消，复查发现测试夹具把导航触发的第二个读取也放进同一个屏障，覆盖释放句柄；已修正测试夹具仅暂停 context 请求，没有放宽产品断言。两份原始日志均保留。全库基线导入的首次 diff --check 曾报告原有历史日志空白，未清洗历史证据；本轮只验收相对原树的增量。

本地实际 Chromium 尝试在页面导航时被环境浏览器策略拒绝（ERR_BLOCKED_BY_ADMINISTRATOR），没有通过任何新浏览器断言，也没有更改该策略。保留失败日志，将新的真实浏览器/数据库验证接入既有 CI。不得把 VM 单测、该本地尝试或父提交绿灯称为本轮真实 Chromium/PostgreSQL 通过。

## 待执行及验收门槛

新 fixture 创建独立合成来源，明确写入夹具的全局/不同项目、active/candidate/archived、大条目后，由 Chromium 只读操作管理台。比较前后5张表指纹；验证实际项目过滤、预算不截断、文本不执行、导出不带任务、取消/锁定/导航及回执指纹变化。CI 仍运行原 Chromium 管理台与 n8n 工作流，没有替换旧测试。

创建本文时新 CI 与真正独立 reviewer **PENDING**。本记录是实现助手自审，不是独立代理。复审应关联最后完整提交、真实审查标识、具体发现和执行范围；新发现修复后重审新的 SHA。没有公开的 reviewer 细目不编造。未经验证的代码不合入 main，不宣称个人 V1、真实用户部署、跨主体客户端或真实模型质量全部完成。
