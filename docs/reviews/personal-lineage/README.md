# 记忆引用来源核对：实施和验证记录

阶段基线 dcb489829bef6337f4e5572c28dc17adf1e254dc，完整树458a1fb12fc0794af2bec9d62fd9c3f9d62afffa。从已取得的该提交CI源码工件恢复全部跟踪文件和四个gitlink，重建基线整树一致。在隔离工作目录继续开发，未改用户主机、main、已应用迁移或上游锁。

生产新增纯引用合同与独立lineage-ui，index接入口，personal-console只登记两个固定静态资源。直接复用现有memory_read/verifyLookup，不新增数据API、MCP工具、权限或数据库操作。流程是显式同意→根记录→直接来源→重读根记录→区分版本/指纹/UTF16引用/归档→显式全文或现有纠错入口。不是递归图谱、原子读取快照或真实性认证。

初始31项合同测试在新模块尚未实现时全部失败，contract-baseline.log.gz保留，属于缺少新功能，不是31个现有漏洞。功能专项首轮89/89通过。并行全量1661项首轮1660通过/1失败，定位为新UI测试依赖100次setImmediate轮询等待WebCrypto的时间假设，失败点是未到达测试拦截点。改为显式Promise进入信号并保留有界等待及提前结束断言，而非提高旧CI超时或放宽产品断言；原失败摘录和完整日志哈希保留，完整日志随交付包提供。

最终普通账号oai（uid1000）、Node22.16.0执行全量1661/1661，零失败/取消/跳过，完整退出0。新增92项：31合同＋58实际脚本/合成DOM＋2保留既有CI覆盖/顺序约束＋1固定静态路由。Python3.13.5全量683/683，42.584秒，完整退出0（监督过程44.001秒）；Python单测没有修改。JavaScript语法、Python AST、两份工作流YAML及差异检查通过。Node/Python具体原始日志指纹见local-evidence.json。

实现者自审覆盖：源身份与原文匹配、来源不可见和网络失败分离、不绕过共享记录隐藏来源、引用区间中文/emoji/CRLF、来源改动但引文仍匹配、归档、重复读取期间选中记忆改变、每次网络/哈希前后撤回、迟到失败隔离、只读跳转不覆盖草稿、父页面导航和未确认请求边界。实现者自审不是独立代理。

真实集成已加入现有预览工作流，在所有原有文档/任务/恢复/快照阶段之后运行；原跨平台套件全部保留，增加两个新套件。夹具通过真实存储和四次明确注入的合成生成器创建引用，读取阶段核对六表不变；独立并发阶段实际写入一次纠错事件并要求旧引用观察被拒绝。没有新增外部模型费用或生产服务动作。本记录创建时这些真实测试与独立最终SHA复审仍PENDING，必须读取真实结果后再验收，旧dcb的绿灯和独立意见不覆盖新实现。
