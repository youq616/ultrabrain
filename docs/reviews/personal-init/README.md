# 2026-09-19：首次控制台凭据初始化 / 实现复查

## 范围与基线

远程主线基线 `491bf6fd702d8b88d30d65acb23b259a99242fcb`，本地应用来自固定 Source snapshot `6aae8a01f0d5f27dedf64e0687af9cb3b02e29ea`。两者之间仅文档和旧验收归档差异，提交使用远程主线树作为基础，保留所有较新档案。本阶段不依赖尚未批准的 PR #15，不修改该 PR 的 Agent 登记代码或测试；因此本轮 Node 615 是主线 612 + 3，不是上一候选 644 的删测回退。两组改动仍需分别完成独立审查与合并。

功能合同见 [PERSONAL-INIT.md](../../PERSONAL-INIT.md)。不改变现有 PostgreSQL、上游锁、应用迁移、Agent 身份/同意合同、qbrain 或用户实际服务。新凭据代码只落入独立开发分支，不部署到用户主机。

## 实现后的第二轮自审

这是**实现助手自审，不是独立代理审核**。复查覆盖 CLI 路由、隔离 Python、默认 home、私有文件读取、排他创建、短写/同步、同 inode 检验、配置和路径替换、并发与失败后重试，以及控制台与部署/激活凭据兼容。

发现并修正：初稿仅验证落盘结果格式及最终文件签名，如果同步窗口内被换成另一组同样合法的十六进制字节，可能错误宣称这是本次生成值。已用确定性故障注入复现（31 项中 1 失败），增加逐字节原文比对后通过，且不回写或删除被替换的文件。

完整 Python 回归还暴露既有 `personal-deploy` 在解析器构造时提前求值 passwd 默认目录，发生在普通 Linux 账号门槛及脱敏异常边界之前。在本地没有 UID 1001 账号时，既有拒绝测试出现 KeyError。已将默认目录解析移入账号门槛之后，且仅缺少显式/环境 home 时执行；新加 4 个针对性测试，未修改或跳过原有失败测试，也没有为测试添加系统账号。

## 实际执行证据

- 新 public CLI 测试先在原实现执行：3/3 失败，保留原始日志；随后接通命令并通过。
- 初始化 Python 专项最终 31/31，包括实际 CLI、8 进程并发、真实子进程退出、ENOSPC/短写/零写/fsync 故障注入、FIFO/链接/宽权限、原文字节替换和脱敏输出。
- 全量 Node 普通账号：615/615，失败 0、跳过 0。
- 全量 Python 首轮：573 项，1 个既有默认账号查找错误；修复并增加 4 项后：577/577，全部通过。
- 新鲜安装真实集成 `test/personal-init-integration.mjs` 接入现有 `personal-services.yml`，从无 token 开始检验公开 CLI、私有创建、重复使用、配置/数据库进程不变，之后由**测试本身另外启动**真实控制台验证登录，并复用原有服务部署/激活测试。提交本记录时尚未得到本候选 GitHub CI 结果，不能将其标为已执行通过。
- 上述本地命令以普通账号执行，内容与故障均为合成；没有真实模型质量、用户客户端安装或生产部署认证。原始首败见 `first-failures.txt`，完整本地日志和文件指纹见 `local-evidence.json` 及补充日志归档。

## 接受状态

**开发候选；独立 reviewer PENDING；main 不变。** 当前可用入口没有可直接启动的独立 reviewer 会话；之前仓库 Codex 审查明确遇到额度限制。外部插件检索未找到已经连接的替代独立执行器。CI、自审、审核请求或机器人额度提示都不算批准。最终候选 SHA、实际 CI run、审查回复应补记到 PR；修复新的阻断问题后必须审核新的精确提交。

独立审查交接要求：请以未参与实现的独立 reviewer 身份在新干净目录获取本阶段候选 PR 的 head，核对完整提交 SHA 后阅读 AGENTS.md、docs/PERSONAL-INIT.md、docs/reviews/personal-init/README.md 及 local-evidence.json，检查相对 491bf6fd702d8b88d30d65acb23b259a99242fcb 的全部差异，重点复核创建而非轮换、现有部署丢失令牌的拒绝、描述符/字节一致性、写满/崩溃及同步失败不误报成功、CLI 无副作用和部署账号门槛，并查看候选 CI 的首次初始化与既有 systemd 激活结果；只补必要的独立验证，不要求用户安装 WSL、重跑 Linux 全量、提供秘密或操作生产服务，不改 main；在 PR 给出真实 reviewer 会话/run 身份、完整被审 SHA、文件行号发现、实际运行与仅查看的测试分别列明以及明确 verdict，发现阻断则保持草稿并要求修正后重新审查。
