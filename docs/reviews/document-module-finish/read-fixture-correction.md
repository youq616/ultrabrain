# Complete-module CI fixture alignment (no application changes)

Candidate93ea57a33d49fce7bd8898f5d207caa49e75a4e0 failed preview run35514520032 / job106088090534 after the newly added timestamp receipt cases had passed. The full job log was read: document receipt phase10 and its8 events/3 documents/2 fragment/job deltas passed, but the subsequent read-only browser fixture still asserted exactly2 documents, and later login also expected2 cards. The complete module step was skipped in that failed run and cannot be reported as passed on93ea.

The retained CI excerpt has original timestamps and is only an excerpt, not a complete job log. The correction changes the read fixture to assert the exact three known document labels, count, archived state and revision2, then expects that exact set size in the rendered page. It still selects the original two documents by label for all wrong-document, cancellation, late response, fresh-download and byte-identity checks. No permissive >= bound, skipped test or relaxed timeout is introduced. src/web/packages and all production behavior are unchanged from93ea.

Ordinary-account complete reruns: Node1165/1165, zero failures/cancellations/skips; Python683/683 in23.900 seconds, exit0. Browser Python AST passes. Executing just the actual fixture-precondition AST accepts the correct set and refuses six missing/extra/duplicate/wrong-status/revision sets; this is not a Chromium or PostgreSQL test and is not added to official test totals. Real final-SHA full CI and separate independent review remain PENDING at commit creation. Previous three independent findings remain fixed and must be included in final review scope.
