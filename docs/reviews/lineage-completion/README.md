# 记忆引用来源核对：跨工作区撤回与完整验收

本轮承接远端候选 c4ec9b0c8e8a0f2bc52de16406ce261bcc15385b（树 e8456076fd73705e2a70183db11a87d2369c6037），不是覆盖回较旧的快照浏览器提交。完整模块说明见 ../../PERSONAL-LINEAGE.md；保留原来源核对、快照、文档、任务和恢复流程。main 与用户服务不变。

## 独立发现与修正

独立 Codex review 5267519383、comment 4062850569 指出 P2：已核验文件存在时，用户可以先显示引用原文，再点击 explorer-open；该入口不会改变 view/loadVersion，旧委托选择器未包含它，导致来源正文与同意没有撤回。原候选八组 CI 虽然成功，仍不能据此认为这项发现已解决。

本轮唯一生产改动是在 lineage-ui.js 的捕获阶段撤回入口中补齐 explorer-open，并注明不依赖主视图变动。入口自身处理之前就清空记忆正文、引文、来源元数据和原文，撤回同意、清除 ID、终止等待并增加观察代数。迟到响应/错误无法恢复旧内容，已经撤回的读取不再继续后续查询。重新打开来源核对必须重新同意；已核验快照和无关编辑草稿不被清除。没有新增数据操作、模型调用、权限、迁移、依赖或上游锁改动。

## 先复现再修正

增加13项具名 workspace boundary 测试，使用实际 CSS 选择器列表的精确匹配，不再用 closest:()=>true 掩盖漏配的按钮。覆盖三个快照入口、准备/已展示原文、三次读取各阶段的成功与失败迟到响应，以及无关控制不误撤回。在未修改生产代码上运行13项：5通过、8失败，退出1；完整原始输出压缩保存在 first-failure.log.gz。八个失败是同一撤回缺口的边界用例，不是八个独立漏洞。

修正后104项来源核对专项全通过。普通账号 uid1000 / Node22.16.0 完整运行1674项全部通过，零失败、跳过、取消，退出0；专项为全量子集。Python3.13.5全量683项通过，退出0（监督过程79.616秒）。最终远端CI的结果另记PR验收记录，不把未结束的运行当成功。监督脚本在启动前发现并修复过一次本地语法错误，未执行任何测试，不计为产品失败或通过。

## 真实集成补充

现有 PostgreSQL 集成生成一份真实的当前所有者快照，仅保存在mode0600临时文件中并最终清理。真实 Chromium 通过原界面选择、核验该文件，不注入伪造核验句柄。保留原16项来源浏览器检查，追加4项：本地文件准备无数据请求；显示原文后打开浏览器即清除且主view/loadVersion未变；来源读取已完成但交付尚未返回时切换，阻止第三次读取；独立快照仍可浏览且重新打开来源需要新同意。

只读阶段仍核对六张应用表不变、只出现ID-only memory_read、无下载/外部模型调用。另一个独立并发阶段仍保留真实一次更新与精确事件/记录变化断言，不称全程只读。原四次合成生成器调用用于准备派生引用，不是外部模型质量证据。未删旧测试、未更改CI清单或增加超时。

本记录提交时最终CI与修正后独立复审待返回。必须对最终完整SHA获取具体复审结论和全部CI结果，再补记PR20验收；不沿用带P2发现的旧审查，不把实现者自审当独立代理，不编造未披露的执行明细。当前阶段不等于用户主机部署、整套个人V1发布或零缺陷保证。
