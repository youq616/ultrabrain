# Final acceptance follow-up: strict-CSP fixture wait

The first corrected lineage candidate 213b42c51e1025f2cb8b4373b77f6938ac44ff09 obtained an independent no-major-issues verdict (request5769205160/result5769237903). Its first preview CI nevertheless failed: run35669293556/job106561925990 stopped in the existing recovery-barrier browser fixture, before the lineage stage. A successful review is not a substitute for completed CI. Selected original failure lines are preserved in ci-csp-first-failure.log; these are excerpts, not the full remote log.

The failure was an EvalError from wait_for_function's in-page dynamic evaluation under the console's unchanged script-src 'self' policy. The replacement awaits the already-existing primaryWire Promise through a direct async page.evaluate callback, retains a 30000ms bounded deadline, returns the actual wire result, and still explicitly requires ok=true plus completed state. A missing Promise, stalled Promise or failed wire cannot become a successful acknowledgement. The original pending-request visibility and subsequent reconciliation checks are unchanged. It never resends a request or invokes the model again.

No unsafe-eval, CSP bypass, security-header alteration, timeout increase or assertion removal was used. The only additional executable change after213 is to this test fixture; no application/server source changes. Six regressions in the existing portable recovery suite inspect the real fixture callback and execute it in a VM with dynamic string generation disabled, including stale result vs pending Promise, success, failed wire, missing Promise and bounded timeout/cleanup. All six first failed against the original fixture, then passed. This VM test is not real Chromium proof.

The full new commit requires fresh independent review and all existing CI, including the previously unreachable20 lineage browser checks, readonly six-table oracle and separate one-event concurrent correction. The13 lineage revocation regressions remain, for a total19 new Node tests this completion phase. Local final counts/logs are recorded separately after full reruns; previous213 results are historical, not pre-approval of the subsequent final commit. No main merge, user-service changes, new data APIs, migrations, MCP permissions or model configuration changes.

Post-fix ordinary-user verification completed: Node1680/1680, zero failures/skips/cancellations, exit0; Python683/683 in 73.796 seconds, exit0. Combined lineage/recovery-focused subset132/132. This is19 additions over the c4 starting point (13 lineage +6 fixture), not132 new tests. Final exact-SHA CI/review still require separate completion.
