# Personal identity implementation record

Base: `a72bed5cf7f27e42cb3eca6754b4dfe9712870cf` (PR #16 candidate), whose exact source snapshot was downloaded from artifact 10581721075. Archive SHA-256: `ec7c81ddadb4654f831c68cd48f044cd8512a97c187a9eb2ca6683555a1a02e1`.

This is a stacked development phase, not an acceptance or main-branch merge. It preserves PR #16 and does not replace the independent, still-unmerged PR #15 registration fix. Its unit count starts from this base's 615 Node and 594 Python tests, not PR #15's 644 Node tests.

Scope: a public, read-only local database identity command, fixed distro-Python dispatch, bounded psql dialogue, reuse of the established process/installation checks, malformed-output/timeout/identity-race tests and a dedicated real-PostgreSQL CI workflow. No schema, upstream pin, role, model, user client or production-service change.

Implementation self-review found a secondary parser boundary in home paths: libpq commas and ld.so colon/semicolon/dollar syntax. Initial implementation rejected commas/colons but not semicolons/dollar expansion. Added one test with three failing subtests, preserved its exact first failure, then rejected all these characters before process creation. Also moved selector creation before child creation and close it on spawn failure, to avoid leaving a child on selector initialization failure. These are implementer findings, not independent reviewer findings.

Executed locally as ordinary account, Node v22.16.0 and Python 3.13.5: baseline Node 615/615; final candidate Node 618/618 (zero failures and skips); final Python 627/627; identity-specific optimized Python (`-OO`) 33/33. See the accompanying evidence manifest for exact commands, interpreter versions and log hashes; A combined earlier Node/Python run was interrupted during Python; its incomplete log is retained separately and is not counted as passing. The final Python suite was rerun independently to completion.

The 33 new Python tests include actual child pipes and actual AF_UNIX socket files, with synthetic PostgreSQL/procfs bindings. The three Node tests execute the public CLI but do not connect to PostgreSQL. A separate disposable CI job starts the existing managed database explicitly, then performs 12 checks through the public identity CLI: actual Unix/SCRAM/SQL/backend identity, source selection, ambient PG overrides, real SQL blocking/timeout, recovery and absence of leaked sessions. This job has not yet run when this record is written; only returned CI results can establish its outcome. Existing activation/readiness integration remains unchanged and uses its established fixtures; consuming the new CLI in an automatic first-run coordinator remains a subsequent phase.

Independent review: **PENDING**. Self-review, successful CI, review requests and reactions are not a substitute for an actual separate reviewer. Keep the candidate draft and main unchanged unless the final exact implementation commit satisfies AGENTS.md. Record reviewer/run identity only when actually returned; do not fabricate hidden execution identities or executed tests. Review focuses on peer/SQL/backend trust binding, path interpretation, credentials, read-only scope, child lifecycle and real CI behavior.
