# 整理任务管理：实施、自审与独立复审记录

## 基线和提交范围

本轮实际起点为92d840a453001dbc5d1a040203bb2ab696deb501，完整树7b21f77d895e359c6a8714a274b0cd5c90fa578f。该版本已经包含完整文档模块，因此没有套用旧会话中基于6b8的补丁。main保持491bf6fd702d8b88d30d65acb23b259a99242fcb，继续原PR20草稿分支。

曾追加仅用于源码归档的7ff5f17工作流；发现既有source-snapshot后在应用候选2f5e620aee1b343121b93e9f3c034df16f86b7cf中删除了重复工作流，保留原有源码归档机制。未强推或改写历史。2f的完整树ae370374cb420c7e5bb97d5cb5f414e849347a7e已经从实际源码工件恢复校验一致。

## 模块与实现者自审

服务端新增统一状态选择器、只读事务、参数化状态过滤和有界分页。前端接通状态/ID查询、全页合同验证、原文/候选明确追踪、取消回执匹配和模型重试费用再确认。既有取消/处理SQL、来源身份、原文保存、租约和模型权限未改变。无新迁移、依赖或工具名。

实现者检查了来源隔离、最大次数、处理租约、空候选结果、日期规范性、分页/精确ID差异、迟到响应、无DOM事件的条件变化、确认提示前后复核、取消未知交付和独立草稿。自审还发现新增CI任务曾被放在运行环境初始化之前，改到Node/Bun/数据库/浏览器安装及既有文档验收之后，并加静态顺序回归。这是实现者发现，不是独立代理意见。

## 真正独立的P1发现与修正

独立Codex请求5750981289审查2f5e620aee1b343121b93e9f3c034df16f86b7cf；review5261033178、内联4057395465指出client-portability.yml漏保留原有personal-document-module.test.mjs与personal-document-read.test.mjs，属于P1。虽然该候选8组CI均成功，测试覆盖被缩减仍是阻断，不能用绿灯或Completed状态冒充批准。

先添加固定基线全套文件名断言，在未修工作流上实际运行得到0通过/1失败；原始短日志为reviewer-first-failure.log。然后把两组文档测试补回，同时保留两组新任务测试。基线列表在测试里独立固定，不从当前工作流动态复制期望值。新断言还检查无重复测试以及Linux/Windows矩阵仍存在。修正仅涉及工作流和回归测试，不改变2f应用源码。

本记录创建时最终提交尚须新CI与独立复审，均为PENDING。2f的COMMENTED审查带P1，不是批准；后续结果必须绑定修正后的完整SHA。reviewer内部会话、执行测试等未披露信息不得编造。

## 可复现证据与执行限制

工作容器中断后，从GitHub固定源码工件重新恢复并校验基线和应用树，再完整执行验证，不依赖丢失的临时目录。先前73项中59失败的开发记录不冒充这次重跑；本次把提交中的82项任务测试放到完整92基线重跑，得到16通过/66失败。它们是尚未实现功能的边界组合，不是66个独立漏洞。first-failure-summary.json记录实际原始日志哈希及摘录；完整原日志随最终证据包保留，测试源码已在仓库，可重新复现。

修正后普通账号oai、Node22.16.0、umask022完整Node1248/1248，零失败/取消/跳过，退出0；任务专项83/83是全量子集。Python3.13.5完整683/683，40.860秒，退出0，未修改Python单测。额外重验证时有外层时限中断的尝试，没有完整结果，不计通过；保留部分输出，最后由有界监督进程取得完整退出0。当前原始日志哈希及源码blob见local-evidence.json，不把本地执行归给独立reviewer。

## 真实集成归属

2f候选的实际browser-postgres作业106107391792（run35521932262）已通过原有浏览器阶段和完整文档模块，随后通过16项任务浏览器、20项PostgreSQL/原生MCP检查。用三个明确注入的合成生成器调用验证完成、失败和取消写回隔离；没有外部模型调用。六表读取快照、所有者/跨来源隔离、真实stdio目录与调用、20+3任务分页均验证。浏览器只取消一个任务，其他任务以及五张其他应用表不变，原文仍保留。

上述是应用候选的历史结果，不预先声称工作流修正后的完整最终CI已经通过。正式模块验收及合并仍需最终SHA全部CI和真正独立复审；PR保持草稿、main与用户服务不变，不宣称零缺陷、用户部署或真实模型质量完成。
