# 自有个人记忆快照：实施与验收记录

基线677eff98a1cb45389414b839ee6c2d631b1fabb5，完整树130cac578d8f7bb49d83170a9bb0b126b86177ca。从固定源码归档和已交付阶段补丁恢复，保留执行位、忽略规则内原已跟踪证据及四个gitlink，整树核对一致。本地候选以原PR20开发分支为基线，尚无本轮远端提交；main和运行服务不变。

本模块仅导出当前所有者全部状态/项目的当前记忆行，不导出他人的共享记忆。现有公共投影及rowView负责服务端记录形状；新的纯JS快照合同由服务端与浏览器共同使用。先设置可重复读只读事务，在同一快照内预检并读正文，最多1000条、紧凑JSON结果8MiB，超限整体拒绝。导出需要独立明确同意，不是当前页拼接、数据库备份或隐式文件/聊天读取。

前端实现单独位于snapshot-ui.js，不继续扩大原app.js，也不替换其恢复或写入函数。同步读取真实会话/来源/导航版本和busy/pending状态，在模块加载、HTTP、哈希和下载边界复核；事件与DOM观察只用于及时清屏/取消，安全判断不依赖观察回调时机。没有新的后台轮询、自动导出或数据库写入。

## 实施者自审和失败记录

新增73项Node回归覆盖服务端参数/认证边界、单一只读事务、全部状态、身份变化/撤销、1000/1001边界、字节超限、共享合同、内容/整体哈希、重复和内部字段泄露、动态模块加载前后授权、取消/锁定/导航、迟到结果与只下载一次。它们是实现者执行，不是独立代理运行。

最初专项65项中41通过、24失败，原因是新的模拟engine漏声明kind=postgres，既有权限守卫正确拒绝了它；修复的是测试夹具，不是弱化权限。完整首败first-fixture-failure.log.gz保留，不把这些失败声称为24个漏洞或旧代码安全问题。其后65项通过，再增加8项实现者边界检查。Python外层执行时限中断与一次未固定umask造成的目录权限拒绝另行保留，不计通过，也不修改原安全规则；最终复跑使用普通账号和umask022。

## 实际执行与待完成门槛

精确本地结果和日志哈希在local-evidence.json中。没有在当前容器运行真实PostgreSQL或Chromium，也不将合成适配器视为真实集成。

新真实CI阶段追加在既有恢复验收之后：构造23条跨状态/项目记忆和另一主体的共享记录；另一个真实事务在预检后、正文读取前更新并插入，当前导出必须仍为原23条，下一次才显示24条新视图。独立超大主体分别验证字节/条数拒绝。实际浏览器加载共享模块、明确下载一次完整文件、拒绝损坏回执与撤回同意后的下载。浏览器阶段六张应用表前后指纹相同；并发写入是前一个独立的明确合成测试阶段，不称全程只读。

创建本文时尚无本轮远端提交，远程CI及独立reviewer均未执行。后续结果必须绑定实际最终提交并记录PR20，不沿用677的通过结论。独立审查不是实施者自审/新目录或CI，未披露的审查者内部会话及测试明细不得编造。发现阻断项须修复并对新最终SHA复审，main不得合入未经验证的应用变更。

范围与限制见docs/PERSONAL-MEMORY-SNAPSHOT.md。complete仅针对定义的自有记忆范围；没有导入/覆盖恢复、密钥备份、原始文档字节或历史回放功能，不宣称V1整套发布或真实用户部署完成。

## 前次本地尝试结束时的实际状态

本地最终Node1396/1396、专项73/73、Python683/683（57.345秒）均退出0；这些不包含真实数据库/浏览器验收。GitHub create_tree被平台安全检查拦截（无法确定请求安全状态），未得到提交；没有改用其他写入路径绕过拦截。远端仍需另行核验，正式CI及独立代理复审没有执行。此目录和随附补丁是本地候选审计记录，不是部署交接指令。


## 本次继续执行（2026-09-21）

用户再次要求完成整个模块后，重新读取PR20，远端仍为677eff98且上一阶段已完成独立复审和8组CI。通过Library取得前次本地候选补丁，先用实际源码归档及既有恢复补丁还原并核对基线130cac5，再应用候选得到原33804d8整树；没有假定本地候选已提交或通过远端验收。原来的安全检查拦截记录保留，未视作成功写入。

本次普通账号oai、umask022重新执行了原候选：Node1396/1396、快照73/73、Python683/683（43.786秒）完整退出0。新一轮实现者复核补充内容读出后撤销权限与三种原文逐字保留测试，并明确界面与说明：不读取凭据文件不等于自动删除已保存正文中的秘密，导出的私有明文需保护。没有使用用户个人记忆作为测试数据。补充4项后本次完整重跑Node1400/1400（零失败/取消/跳过）、专项77/77、Python683/683（41.941秒），全部完整退出0。日志哈希见resumed-evidence.json。实际新SHA的CI/独立结论以PR记录为准，本段不预先认定远程验收通过。
