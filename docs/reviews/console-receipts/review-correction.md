# 独立审查修正：HTTP 成功不能携带确定拒绝

独立 Codex review **5258535272** / 内联意见 **4055400401** 对完整提交 `1bf428d0694dd20c981323b0d2d5fe3b07ef908f` 的 `web/personal/app.js:26–29` 提出 **P1**：完整的 `{ok:false,error,delivery:"rejected"}` 放在 HTTP 2xx 响应中时，初版仍把它当作确定拒绝，可能在服务器已经提交后清除 pending 及稳定事件。该 review 是 COMMENTED 并有阻断发现，不是批准。

先添加7项定向回归，初版 **1通过/6失败**；通过的一项确认正常非成功HTTP拒绝仍被识别，失败项覆盖四类写入、前置登记和同事件恢复。原始失败日志保留为 reviewer-first-failure.log.gz。修正仅在错误封套分支增加 HTTP 成功与否的一致性检查：2xx/ok:false 一律视为未确认；正常非2xx确定拒绝保持原行为。不能据此防止攻击者同时伪造状态码和响应体，不宣称密码学认证。

新增真实 Chromium 用例：数据库确实提交后，在送达页面前改成完整负面JSON但保留HTTP200，必须保留新事件；用户明确重试原事件后，数据库中该条记忆仍是revision1且没有重复条目。保留此前9项回执检查，新回执阶段共10项，目标传输13次含5次重放，数据库增量要求8个事件、5条记忆、1个排队任务、0个文档。原19项只读预览和11项纠错阶段不删改。

修正后普通账号本地 Node **894/894，0失败/取消/跳过**；Python **683/683**，完整退出码0；回执专项 **69/69** 是Node全量子集。相对本阶段基线4b8累计新增69项Node。再次核对语法、Python AST、原始日志及增量diff；未改后端/迁移/上游锁/MCP目录/用户服务。

当前修正版需重新取得完整CI和独立reviewer对最终SHA的结果。初版1bf的9项真实回执浏览器通过不能替代新10项用例，初版review的P1不能被“活动Completed”或部分绿灯掩盖。创建本文时最终CI/复审均PENDING，实际结果随后记录于统一PR #20。reviewer的内部session或执行测试明细未披露时不编造。
