# 完整 CI 修正：旧浏览器场景应明确结束旧编辑

修正候选 `1e074381f03119ff6f36f4c1711e5f954c23606b` 的新回执浏览器及其他7组专项已通过，独立复审评论5746496267也返回未发现重大问题，但主验证 **35479378505 / n8n-engine job105994493304** 失败。已读该作业完整日志：原管理台浏览器在 `test/personal-console-browser.py:151` 等待已经隐藏的 retry 按钮超时；后续真正 n8n 执行步骤没有运行，不计通过。

检查脚本发现旧测试在保存版本冲突后归档记录，再直接填写所谓新建记忆。它依赖旧的“任何审核成功都清空编辑器”副作用。现在审核正确保留独立草稿和旧 editing 对象，因此这个动作仍是旧版本 update，不是测试拦截的 commit。pending-panel 的短暂可见不足以证明已经到达指定的提交后丢回执屏障。新增单元回归验证：冲突后审核保留旧编辑；明确取消编辑后，下一次保存才发送 register/commit。

本修正仅改变测试和证据，不改 `web/`、`src/`、`packages/` 的应用代码。旧浏览器先断言草稿和取消编辑按钮仍保留，再明确点击取消，确认进入新建且保存同意清除。原丢回执用例还新增等待 network_unconfirmed 和真实 commit 拦截计数1，不能再把临时 pending 状态当作目标操作已发出。保留撤销同意阻止重试、原事件不丢失、恢复同意后同事件重放、无重复候选及后续文档/模型未配置检查，未扩大超时或删去断言。

原日志选定失败行保留为 `legacy-browser-first-failure.log.gz`；这是带原时间戳的摘录，不是完整作业日志。完整来源为上述Actions run/job，摘录哈希在附属JSON中记录。

最终普通账号本地 Node **895/895，0失败/取消/跳过**；回执专项 **70/70** 是全量子集。Python **683/683** 在独立监督的完整重跑中38.913秒结束、退出0。一次外层60秒时限中断的附加Python尝试另行保留，不计通过。语法、Python AST和增量diff检查通过。阶段累计新增70项Node，Python单测未更改。

修正后必须重新取得精确新SHA的完整CI和真正独立复审，创建本文时二者PENDING。旧1e的专项通过和review结论不能当作新提交已验收。原管理台浏览器现在定义26项检查；回执专用19/11/10三阶段仍全部保留。主线、用户服务、权限、上游锁、数据库及已应用迁移保持不变。
