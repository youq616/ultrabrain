# 本阶段 CI 修正：精确核对新增后的客户端工具目录

初始候选 `f5ebbca58e855dd01ee9d4227c9a151559b50dee` 的7组专项工作流成功，独立 Codex 评论5743381430对该SHA返回未发现重大问题；但完整 Validate run **35454069257** 最终为 **failure**，不能把独立审查或部分绿灯当作完整通过。

15个历史升级分支的客户端集成步骤均未成功。已阅读其中 job **105926155375**（升级基线57852fed3d1839bab105c5b56632af1f65bb4b15）的完整原始日志：旧库升级、权限、真实stdio/HTTP和个人记忆检查在该作业已通过，随后 `test/client-kit-integration.mjs:39` 仍断言8个只读工具，而新增 `ultra_memory_read` 后返回9个，出现 `9 !== 8`。同一测试后面还有旧的14个采集工具计数，此项在上述作业因前置失败尚未执行，不能声称已观察到它的运行失败。

`ci-catalog-first-failure.log.gz` 保存该完整日志中的原始失败片段（含原时间戳与堆栈），不是完整作业日志。完整来源是仓库Actions run/job所示日志，不把手工提取片段标为原日志全本。

修正只涉及测试和证据：新增独立于生产allowlist的固定期望名单，分别是9个只读工具及15个含采集操作的工具；实际客户端集成按完整名称集合精确比对，仍保留对SQL、模型整理和禁用写工具的拒绝检查。不使用 >=、不删除失败用例、不动态复制生产列表当期望值、不更改应用权限。单元测试复用固定合同，在明确SDK doubles中校验两种模式，便于在无数据库阶段发现此类目录变更遗漏；实际SDK/数据库证明仍必须来自新CI。

修正后普通账号本地全量 **Node 781/781，0失败/取消/跳过；Python 683/683，退出码均0**。客户端请求专项23/23。相对阶段基线feb7累计新增47项Node；原44个读取/界面专项不变。再次检查所有变更语法及增量diff，应用目录src/web/packages的内容与f5候选完全一致。新的测试和证据文件哈希见ci-catalog-correction-evidence.json。

创建此记录时新候选CI和独立最终复审仍待执行。f5的19项只读浏览器、6项明确纠错浏览器、24项数据库精确读取及17项已安装代理结果是已执行历史证据，不冒称其来自修正提交。必须等待新SHA的完整矩阵和独立复审；main与用户运行服务继续不变，不宣称已排除所有问题或个人V1发布。
