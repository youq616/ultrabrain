# 精确记忆核对阶段：开发与实现自审

基线为统一草稿 PR #20 的 `feb7d69e93c61c98047fb1d7267d4120a4b88355`，完整树 `327cb910897ca852a74ebfa294478af237f73691`。本地从固定快照及此前已交付补丁恢复后核对整棵树一致，保留所有原有执行位、历史证据和4个 vendor gitlinks。本轮非强推追加到同一开发分支，不修改 main、其他项目、上游锁、迁移或生产服务。

## 本轮开发

先核对既有搜索只做正文子串查询，不能凭召回 ID 精确找到记录；随后添加唯一 Store.read 合同及只读 MCP/管理台映射，已安装客户端同步支持转发。界面重新读取单条记录，显式编辑才采用新读版本；不直接用旧预览启动更新。所有写入仍由旧的 consent、事件重放与 revision CAS 处理，没有另造写入路径。

自审逐项检查参数拒绝、服务端来源/主体、所有者状态范围、非所有者 active/source/派生有效性、非所有者引用隐藏、只读事务和超时、完整条目上限、错误不假装空结果、取消和迟到响应、异步哈希后的身份条件、草稿保持、共享/文件片段不可编辑。补上绕过 DOM 事件改变 ID/会话/来源、旧请求覆盖新请求、所有者失效派生仍可核对等5项回归。没有在该检查范围发现尚未处理的阻断项，但这只是实现助手自审，不是独立 reviewer 的批准。

## 实际本地证据

普通账号 Node v22.16.0：**779/779**，0失败/取消/跳过；系统 Python 3.13.5：**683/683**，完整退出码均0。Node使用 --test-concurrency=4 限制本地测试进程，未改变产品参数或断言；Python单测未修改。相对基线734新增45项Node：14个Store/插件、30个浏览器JS、1个客户端只读转发；新增 read/lookup 两个专项文件共44/44。AST/Node语法及增量 git diff --check通过。

初始特性测试39失败，其中14个因模拟 engine 遗漏 kind 字段；首次实现的100项组合运行86通过、14失败也是同一夹具问题。补齐测试适配器的真实 kind 后，未放宽生产门槛。为避免夸大缺陷，又在干净的完整基线树运行最终44项新特性回归，**44失败/0通过**；这是功能不存在的首败，不是44个安全漏洞。完整该次输出在 first-failure.log.gz，初始夹具失败日志仍作为补充证据保留。

首次不限制并发的本地Node运行被外层执行时限中止，没有完整结论。保留原日志，未计通过；随后独立监督进程运行完整测试并记录退出码。增加自审回归之前的774项成功也只作为历史记录，最终统计以779项原始日志为准。文件及日志哈希见 local-evidence.json，完整本地日志补充附件不替代已入库源码。

## 集成与独立审查

新增真实数据库/原生dispatch读取矩阵；既有 stdio/HTTP 测试增加精确读、候选隐私和共享；已安装代理增加候选/纠错/归档精确读及撤权拒绝。Chromium第一阶段扩展到19项只读检查，前后5表不变；第二阶段6项明确纠错动作并核对最终记录为revision6、candidate、保留并发新值，不是“全测试无写入”。这些是待候选CI实际执行的检查，创建记录时不计为通过。原有n8n、部署、恢复和Windows矩阵保留。

创建本记录时当前候选真实CI与独立最终审查均 **PENDING**。新候选需要实际 separate reviewer 对精确SHA的意见，发现阻断项修正后重新审核；旧SHA的绿灯或自审不替代。公开review会话或测试明细缺失时如实记录未披露，不能编造。未经验证的应用不合入main，不宣称个人V1全部完成。
