# Personal service units — implementation self-review

This is the implementation assistant's separate inspection, NOT an independent reviewer. An actual independent reviewer and final-candidate CI remain mandatory. No self-approval or production deployment is claimed.

Scope: inactive unit bundle generation and verification, CLI routing, exact source/home binding, explicit scheduled-model permission, timer/one-shot semantics, path/specifier/environment encoding, private exclusive output, partial writes and checksum validation; affected existing preflight, PostgreSQL manager, console, Worker and model-consent contracts. No migration, upstream lock, capture permission or enterprise tool changes.

The first actual `systemd-analyze verify` run rejected the initial WorkingDirectory double quotes: this setting treats them as path bytes rather than shell quotes. Corrected to a literal absolute path with escaped specifiers, rejecting ambiguous backslashes/colons/trailing whitespace; command arguments retain their distinct quote/expansion handling. The old failed parser log is retained outside the repository, not mislabeled as an environment issue or test success.

A subsequent inspection added a pinned executable PATH for preflight inside systemd (the user's interactive Bun location is not assumed in the service manager PATH), an explicit PrivateUsers setting for namespace-based user-service hardening, a settings snapshot before preflight, consistent authority flags in the manifest, and strict real-test cleanup. Unit tests use fake preflight only where file safety is isolated and say so. The real systemd/DB/model-fixture path is a separate gated CI requirement, not replaced by these tests.

Acceptance record must name exact final commit/tree, actual test counts, independent reviewer identity/outcome, any later failures and their corrections. No version percentage, no blanket claim of zero defects, no final Personal V1 declaration. Platform namespace restrictions are not silently relaxed to make a test pass; the actual application and model contracts are reused.

Local validation before first candidate: 487 Node tests passed (the first full run exposed missing CLI dispatch wiring for the new command; all four new CLI tests failed then passed after fixing routing). The 32 new Python unit tests passed, including actual systemd parsing. One combined test command exceeded its outer terminal timeout after Node completed; the Python test process finished its own report afterward, so the outer command is not called successful. The container does not have a usable systemd user manager; actual user-service integration must execute in the dedicated CI job, without weakening namespace restrictions. Logs are retained privately; no user data was involved.
