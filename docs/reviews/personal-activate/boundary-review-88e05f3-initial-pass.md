# Final independent activation boundary review

**Verdict: PASS for the entire implementation phase at exact commit `88e05f3f84d1cd1ac2e3fdf2f22237c84c6cb3c4`. No unresolved blocking code finding remains in this review.** The affected local suites and both actual ordinary-user services CI runs passed. This is an independent review decision for this exact candidate; it does not replace the coordinator's remaining required CI/merge checks or claim deployment to the user's host.

Actual principal reviewer: `/root/activate_boundary_review`. Supplementary independent source and recovery reviewer: `/root/activate_boundary_review/dbus_abi_crosscheck`. Neither reviewer implemented or edited repository code. The supplementary reviewer is not the earlier `/root/activate_recovery_review` session. The coordinator reported that attempts to reactivate that earlier session reached an agent-thread limit; this tool limitation is not a review result. The identities and work actually performed are recorded separately below.

- Repository: `/workspace/scratch/be43dca0ab34/ultrabrain`.
- Phase base: `5b3c88826984a9b5c8b34ca2cf7ffff171501697`.
- Reviewed commit: `88e05f3f84d1cd1ac2e3fdf2f22237c84c6cb3c4`.
- Reviewed tree: `f8f540288ffcc5de31b865588130528f05130c71`.
- Immediate parent: `9f4e5c2ac81250b7025234fd61008d0bc92e31ef`.
- Actual PR test merge: `bdb287727eed2c574385aded849e8b923bf71fda`, with the same tree and parents equal to the phase base and reviewed commit. I independently checked the GitHub Git-commit API; I did not rely only on the implementer's audit.

Review performed on 2026-09-18 UTC. HEAD and tree were verified, with a clean worktree. The phase-wide `git diff --check` passed. Review evidence and test harnesses were written outside the repository in `activate-evidence`; all earlier review reports and first-failure logs remain unchanged.

## Scope and findings

The decision covers the full 16-file phase diff: activation coordinator and manager adapter, deployment lock/reservation composition, readiness composition, CLI/package routing, documentation, the ordinary-user workflow, activation/CLI fixtures, deployment integration, and the bounded capture concurrency fixture. I reviewed the new four-file delta on the exact candidate, reread the complete manager and diagnostic/activation integration boundaries, and rechecked the phase-wide composition/workflow/docs changes. Earlier exact-candidate boundary analyses are carried only where the code is unchanged. The supplementary reviewer independently reread all 499 coordinator lines plus persistence, locking, readiness/process and CLI boundaries, rather than treating implementer tests as independent review.

1. **Passive swap names are accepted without authorizing swap execution.** `scripts/personal_activate_manager.py:64–66` adds `.swap` to the bounded unit-name grammar used for cached facts. `names` at `141–145` still requires a bounded list of exact strings with no duplicates; `_read` at `348–361` continues canonical Id/Names and cached-property validation. The common traversal reader at `433–448` rejects both `.device` and `.swap` at line 441, before `_read`/LoadUnit at line 444. Every start, stop and verify entry passes it at line 456. START edges, conflicts, verification and reverse STOP edges remain explicit at `458–471`. Passive names remain in the parent's contract, without entering `units`, runtime observation, or transaction membership. The repeated cache read at `501–505` still detects changes in these passive facts.

   This fixes a source-confirmed compatibility defect without reopening the original hidden-followers blocker. Fixed systemd v255 commit `db11bab38ccf1ed257f310d29070843d4c58ea01` enumerates `/proc/swaps` for user managers; extrinsic swaps can require/order after the root slice. RequiredBy has no ordinary START pull-in atom, ordering considers existing jobs, and following-set expansion belongs to a unit actually receiving a job. The slice has no following-set callback. Reached device and swap units nevertheless remain unsafe because empty displayed Following can conceal siblings. Sources: [swap enumeration](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/swap.c#L1336), [slice dependencies](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/unit.c#L1516), [dependency atoms](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/unit-dependency-atom.c#L16), [transaction following/propagation](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/transaction.c#L1012). The supplementary primary-source report gives the full proof, including why active-state redundancy is insufficient. Its observed manager blob `12e5ad8589d98e147612ff25ad8dc8c637d478a3` exactly matches this final candidate.

   I executed the checked-in 52 manager tests, including the original canonical-device/hidden-follower regression and the new swap tests at `test/test_personal_activate_manager.py:301–346`. I additionally checked all 24 device/swap reachability combinations across four START, two conflict, one verify and five STOP properties. Each refused before LoadUnit and made no StartUnit call. Six passive RequiredBy/Before/After cases retained the name without loading the device/swap, its synthetic sibling, or Worker. A second-read passive-swap change was refused. These extra fixtures exercise the real final adapter with a controlled bus; they do not claim a real device/swap transaction was executed.

2. **The manager, sender and single-dispatch boundaries remain intact.** The fixed-address ordinary-user connection and same-UID manager process verification remain at `scripts/personal_activate_manager.py:193–234`; identity rechecks and the departed-sender fence remain at `285–326`. The fence requires the exact old-sender NameHasNoOwner outcome, a reply to Ping addressed to the saved manager owner, and unchanged identities, under the deployment lock. The supported v255 synchronous same-UID authorization assumption has not been widened. `_raw_call:255–270` retains disabled implicit activation/interactive authorization, positional timeout for the real dbus-python C ABI, and reply sender/serial/signature validation. `start_console:552–566` still sends only the fixed console START/fail request once and reports any post-dispatch failure as uncertain. No new recovery start, stop, restart, reload, enablement, model call, or privileged fallback was introduced.

3. **Durability, recovery and readiness retain their earlier protections.** `scripts/personal-activate.py:43–89` uses the existing checked account-global lock/reservation and closes the bus before releasing the lock. `345–391` binds the exact plan, writes intent and reservation, reobserves state, persists attempt before the sole call at line 373, then separately stores acknowledgement. `185–269` validates typed exact record schemas and cannot invent a successful dispatch from missing records. `310–335` commits receipt before history and pending cleanup. `406–436` fences before observing or completing a historical receipt; it never redispatches. New ready results require fresh authenticated readiness with repeated installation/process/invocation bindings. Historical receipt completion returns `application_ready: "not_checked"`. Public readiness remains under its own shared lock; internal composition requires a checked held lock (`scripts/personal-ready.py:308–358`). Deployment rejects the same activation reservation (`scripts/personal-deploy.py:344–358`). The supplementary report records detailed file/line review and actual state/ready tests.

4. **The new plan diagnostic is gated, bounded and read-only with respect to service actions.** `test/personal-activate-crash.py:369–400` retains the Linux, ordinary-UID, exact CI flags and fixed disposable-home checks before loading the application. The new command calls `Context.plan` exactly once at `322–359`, restores the original `_all` method and closes the manager on success and failure. Its wrapper examines only Unit-interface `Names` and the fixed EDGES list; it returns the original response object, and it neither logs Service GetAll contents nor changes the production rejection. The only added output at `297–319` is one of fixed unit/property/type/suffix categories, booleans/null and length capped at 513. Rows cap at eight across repeated reads. Error types/codes/frame names remain fixed allowlists with at most six contexts and eight total frames; raw exception text, unit names, paths, arguments, plan contents and token values are not printed.

   `test/personal-activate-integration.mjs:139–185` bounds child output to 16 KiB and validates the complete dependency schema and fixed enums before printing it. Its original plan error is rethrown at line 269 after diagnosis. My controlled tests include secret-bearing names/objects and returned plans, missing/mixed/oversized arrays, duplicate/unknown suffixes, constructor and plan failures, object identity preservation, service-property exclusion, eight-row truncation, and actual root refusal before argument processing. Sixteen additional checks use the actual extracted JavaScript validator, including rejection of extra raw fields and preservation of the original thrown error's object identity. No mock authorizes a real manager call or bypasses the public root guard.

5. **The CI environment adjustment does not weaken production identity verification.** `.github/workflows/personal-services.yml:70–87` checks exact disposable ordinary-user context and a bounded visible cgroup path outside that user's manager service. `91–124` binds SUDO_UID, walks root-owned non-writable directories through no-follow descriptors, and creates only the fixed runtime drop-in with exclusive/no-follow creation and synchronization. It cannot overwrite an existing file. Loaded bounding/ambient capability settings are checked before the single bounded restart at `125–135`. The subsequent wait at `141–166` repeats only read-only diagnostics, at most 20 attempts within 35 seconds, solely for the precise not-yet-owned manager name/unbound-process case. Every other identity failure refuses; success still requires the production identity proof and actual empty manager CapPrm. The existing code and its earlier c6/9f adversarial evidence are unchanged. This candidate's two actual CI runs verified the resulting manager identity and empty CapPrm on attempt two; attempt-one NameHasNoOwner remains in both raw logs. The visible cgroup check is scoped to this hosted fixture and is not an isolation guarantee against arbitrary hidden namespace or privileged changes.

6. **Real crash and cleanup evidence now supports the intended scope.** The test-only sent-before-reply callback at `test/personal-activate-crash.py:415–437` verifies the fixed StartUnit arguments, queues and flushes a real message, then exits before reading a reply. It does not fabricate acceptance. Recovery assertions at `test/personal-activate-integration.mjs:201–230,303–346` retain `outcome_unknown`, check current readiness/terminal state, preserve an existing invocation and never send a replacement start. Both real CI8 runs exercised all seven process-exit points. The unread-reply case was observed ready in both; its possible manager-refused terminal outcome was not exercised in these real runs. The after-start-before-ack case had already read the reply but not persisted the acknowledgement. Historical-receipt cases completed cleanup without claiming fresh readiness. Existing RefUnit cache pinning remains bounded and explicitly released around activation integration (`test/personal-deploy-integration.mjs:163–175`), preserving the first activation error if cleanup also fails.

7. **Adjacent boundaries stay within the phase.** `src/cli.mjs:38–44` dispatches the fixed Python script through an argument vector and the isolated distribution interpreter; no shell or public fixture selection was added. The canonical service generator, upstream pins, migrations, server-derived source/principal logic and Worker consent paths are unchanged. The capture change at `test/capture-outbox.test.mjs:96–135` is a fixture-only bounded retry of the same event for `outbox_busy`, with fixed diagnostics and exact eight persisted IDs; production outbox behavior is unchanged. This file's earlier 071b7f independent contention evidence is carried, not represented as a new local run. The exact current PR's full unit CI passes as additional coverage. Documentation accurately limits supported manager/proc prerequisites, the observed cache contract, cooperative maintenance, historical readiness, synthetic provider calls and user-host deployment claims.

## Executed tests and independent evidence

There are **262 affected Python tests and 14 Node CLI tests freshly executed on this candidate**, divided by actual reviewer as follows. These include controlled manager/process/HTTP fixtures and real private file operations; they are separate from actual services CI.

| Actual reviewer | Command | Result | Log |
| --- | --- | --- | --- |
| `/root/activate_boundary_review` | `python3 -B -m unittest discover -s test -p 'test_personal_activate_manager.py' -v` | 52 passed | `boundary-88e05f3-manager-tests.log` |
| `/root/activate_boundary_review` | `python3 -B -m unittest discover -s test -p 'test_personal_deploy.py' -v` | 87 passed | `boundary-88e05f3-deploy-tests.log` |
| `/root/activate_boundary_review` | `node --test test/personal-activate-cli.test.mjs test/personal-ready-cli.test.mjs test/personal-deploy-cli.test.mjs` | 14 passed | `boundary-88e05f3-cli-tests.log` |
| `/root/activate_boundary_review/dbus_abi_crosscheck` | `python3 -B -m unittest discover -s test -p 'test_personal_activate.py' -v` | 52 passed | `recovery-88e05f3-state-tests.log` |
| `/root/activate_boundary_review/dbus_abi_crosscheck` | `python3 -B -m unittest discover -s test -p 'test_personal_ready*.py' -v` | 71 passed | `recovery-88e05f3-ready-process-tests.log` |

The principal reviewer additionally executed `boundary-88e05f3-adversarial.py` (42 passing checks: 31 graph-boundary cases and 11 diagnostic/lifecycle/gate cases) and `boundary-88e05f3-plan-schema.mjs` (16 passing checks against actual extracted JavaScript). These are independent scratch checks, not extra checked-in test counts. The former complete unittest log ends with `Ran 42 tests` and `OK`; the latter returned exit 0. There was no failing candidate test in these runs. A separately preserved expected negative control loads the exact previous 9f4e5c2 manager and reproduces `invalid_manager_dependencies` on a passive root-slice swap fixture before any swap LoadUnit or StartUnit. This validates the compatibility regression; it does not retrospectively identify the unlogged exact dependency value that caused CI7.

## Independently verified actual CI

I directly parsed and asserted the raw logs, then queried GitHub's Git-commit and workflow-jobs APIs to verify identity, tree and final status. The implementation agent's `CI-EIGHTH-SERVICES-AUDIT.json` is retained as implementation-agent triage, not relabelled as an independent review.

| Run/job | Independently verified raw locations | Result |
| --- | --- | --- |
| [PR services 35370431603 / 105682842046](https://github.com/youq616/ultrabrain/actions/runs/35370431603/job/105682842046) | Checkout merge at lines 125/128; manager attempts 522/523; legacy receipt 535; final receipt 547 | GitHub job completed/success. 18 activation, 15 deployment, 12 readiness, 18 legacy checks; two local synthetic provider calls. |
| [Push services 35370427343 / 105682825972](https://github.com/youq616/ultrabrain/actions/runs/35370427343/job/105682825972) | Exact head at line 112; manager attempts 506/507; legacy receipt 519; final receipt 531 | Same complete result and seven crash points; completed/success. |
| PR full unit job 105682838275 | `ci-unit-88e05f3-pr.log`: matching merge at 122; Node totals at 3523–3528; Python summary/OK at 4083/4085 | 563 Node passed, zero failures/skips; 542 Python passed. This is independently read CI evidence, not a local execution by this reviewer. |

Each actual services receipt reports zero activation/readiness model calls, unchanged configuration and database identity checks, actual deployment flock exclusion, refusal when the source is removed after dispatch, and the same console after source restoration. The seven checkpoint outcomes in both logs are: after_journal and after_attempt terminal_stopped; after_send_before_reply, after_start_before_ack and after_ack observed_ready; after_receipt and before_clear_pending receipt_completed. Every checkpoint records extra_starts 0. The low-level unread-reply case keeps outcome_unknown/client_waited_for_reply false; no real NoReply exception was induced by these runs.

## Preserved failures, inheritance and limits

The original device hidden-following-set P1 against preliminary tree `f203331b8ead055d57d1b72d4dc3eed439bdb5dd` remains recorded, along with the failing regression before its fix. Earlier immutable reports remain unchanged: 9e85892 and 071b7f4 code reviews; def34fb's actual process-identity BLOCK; 70cd4cc and 80d6314 diagnostic reviews; c6b409c and 9f4e5c2 actual CI BLOCK reports. First CI failures for inactive-unit cache reset, C-binding keyword ABI, proc read denial/capability mismatch, manager-name registration and dependency parsing remain in their original logs. The old capture failure's discarded child error code is still unknown. Passing CI8 does not reconstruct missing historical values or rewrite earlier verdicts.

The carried fixed-source evidence includes the supplementary v255 dependency proof, prior dbus-python 1.3.2 C ABI proof, and Linux ptrace READ_FSCREDS/namespace review. The prior c6 root-writer fixture checks and 9f wait/enum checks are not counted as freshly rerun here; their relevant code remains unchanged and I reread those boundaries. Current actual CI verifies the corrected environment plus the production proof.

Neither independent reviewer executed sudo, installed dependencies, bypassed the ordinary-user guard, or started/stopped/restarted a real service locally. The actual service changes were the explicitly gated disposable CI fixtures. No user host was deployed, no Windows client was tested, and no live model-quality claim is made. A real manager refusal after unread send and a genuine transport NoReply exception remain controlled-fixture coverage, not outcomes observed in these two real runs. The proof is for the supported systemd v255 contract and cooperative maintenance window, not a security boundary against arbitrary same-UID actors or a claim about every cached property. Other repository CI matrices were still being tracked by the coordinator when this review was finalized; this PASS does not waive those gates or itself authorize merging a failed check.

## Evidence hashes

All following paths are relative to `/workspace/scratch/be43dca0ab34/activate-evidence`.

```text
badaba6c9952fc79da564d59872317391a28b64e80910d7de495a04e02879e74  boundary-88e05f3-manager-tests.log
42f112e951ca30584d862681c90d275d6dff1e4e8a8b3043781a915d0ec4c6b8  boundary-88e05f3-deploy-tests.log
5ca645bc0aa29f9ea5da5b59465bd849e66e5f05e8cc97e17f00f7dd01c866b8  boundary-88e05f3-cli-tests.log
2536623f36cce7351c3e9489f68808832a783b899f63c173227aa49e9d1ece74  recovery-88e05f3-state-tests.log
0d31a116a675eebff4ed460d6cb49cc0c1d1b61e50fdcd9d425e94df84ed6726  recovery-88e05f3-ready-process-tests.log
98379733264d531508d0364cd7219f45c82652669863382124cd40791f43df49  boundary-88e05f3-adversarial.py
81db6e4b30800a83af41066095123d9a19c00b0fe551d0757242856756defd7d  boundary-88e05f3-adversarial-tests.log
51caa908f5abe88035d5cd83f879460ab988155a9cb113c0dc71db6c41c6a37d  boundary-88e05f3-plan-schema.mjs
af8845dd62cefc0fea5a0187751db5b59cf38e5c2a38f6b40dee346e7150074d  boundary-88e05f3-plan-schema-tests.log
b39c02dfd94fb02c5c57b60e2148efcc5813b96ad9ebfcfed813ad8a7cf5952e  boundary-88e05f3-plan-safe-envelope.json
d2bc972b7ca6937356d57547729cdd5d5f5a4de99766db07882a6024a6f600cb  boundary-88e05f3-old-swap-negative-control.json
c228a68080e5b569febb764c29b22f46001d89e69a170fdedc84e55d4356cd5a  boundary-88e05f3-github-identity.json
b9eb1299cf72397a069aa3329cd935ef62a39d498d10ccf44893ce7ae11fd38b  boundary-88e05f3-ci-raw-verification.json
44990e32ae62bdb38bf69a990bc2cb77c96005a6608df7bf0bafc46474b3dc45  boundary-passive-swap-source-review.md
5b661f8c5122223e1f62b50ea2e23dd823532701a46b5d4af8b2b83630a28026  recovery-boundary-review-88e05f3.md
95d90f43e9f704ee8e412453c0057bdbdb5f3a67f66e3299039e975a95fdb16b  CI-EIGHTH-SERVICES-AUDIT.json
ef3d117419f5d65f7bd0ce31baa0fe72c95172405596b1c22cc3e6314f8d29a5  job-105682842046.log
41167fd31495ae8d4239ec09d0baf7eef664b39fe0f100bb6474add298ab233e  job-105682825972.log
af15e2e753fbca9c70a6758df4c86e80d0c85989fe3634fd4bb3f9476f30c42d  ci-unit-88e05f3-pr.log
```

Final four-file delta SHA-256 values, obtained from the committed blobs:

```text
895207d98713974574bc433893d7be90c02c6341f9e18aa332b9e168047eb594  scripts/personal_activate_manager.py
8ae85791243b70c12a9db043f62430ea7b5d65b8c277d45c34ce16c497fce222  test/test_personal_activate_manager.py
54ecc33fbafdd1bba984120e7c8e343ea5869b530749fd70c9a63d93519487c0  test/personal-activate-crash.py
bdc4a51397b5381f9f4802c095cbb481f3cc7238448297ce09d40f54aceea21c  test/personal-activate-integration.mjs
```
