# Personal activation independent recovery review

**Code-review verdict: PASS** for the complete application commit **`9e85892c54006dedfc7dda2135de3b8f90155347`**. No unresolved implementation blocker was found in this reviewed candidate. This verdict does not replace the repository's real CI gate or establish user-host deployment.

- Actual reviewer session identity: `/root/activate_recovery_review`.
- Review completed: 2026-09-18 15:14 UTC.
- Repository: `youq616/ultrabrain`, branch `development/personal-activate`, draft PR #13.
- Full reviewed commit: `9e85892c54006dedfc7dda2135de3b8f90155347`.
- Reviewed tree: `10495f79e8e1d88e0c72e8bc5126bbf97f6fb045`.
- Parent/base: `5b3c88826984a9b5c8b34ca2cf7ffff171501697`.
- Local checkout: `/workspace/scratch/be43dca0ab34/ultrabrain`; HEAD/tree were checked directly and the worktree was clean before and after the final test runs.
- Independence: this reviewer did not implement or modify application code, tests, workflow or product documentation. The reviewer wrote only review reports and test-output logs under `activate-evidence`.

## Scope and concrete findings

I read `AGENTS.md` and independently inspected the entire final change, including the new coordinator, direct D-Bus manager adapter, state/manager/CLI tests, real process-exit integration helper and integration suite. I also inspected the affected existing deployment lock/pending paths, ready composition, descriptor-bound filesystem Store, process verification and CLI dispatch, plus the workflow and documentation. The 15-file final diff changes no migrations, upstream locks, model configuration, memory-store contract or Windows qbrain files.

1. **Shared coordinator ownership and durable ordering: PASS.** `scripts/personal-activate.py:53–89, 345–377` uses the existing account-global lock and sole shared pending slot. `personal-deploy.py:182–226, 344–357` keeps the existing lock identity checks and rejects activation reservations; older deployment schema validation also fails closed. The immutable intent is published and synchronized before the shared pointer; attempt is durable before dispatch; acknowledgement is written only after the actual reply. A crash before pointer publication leaves an orphan without authority to start. The filesystem primitives in `personal_deploy_store.py:185–238, 263–292` use canonical private files, descriptor-bound no-follow walks, no-replace publication and directory fsync. Completion in `personal-activate.py:310–343` commits receipt, history pointer and finally pending removal in that order.

2. **Single dispatch and observation-only recovery: PASS.** `personal_activate_manager.py:548–562` sends only the fixed console StartUnit, bound to the selected manager's unique owner with job mode `fail`. It marks the one-shot dispatch before sending; a lost, malformed or mismatched reply remains uncertain and cannot trigger a retry. `personal-activate.py:378–391` bounds readiness retries while keeping persistence failures outside the retry catch. `personal-activate.py:406–436` performs no StartUnit, StopUnit, restart, cancel, enablement, reload or configuration compensation during recovery. Cross-home reservations, wrong pending digests, live old senders, identity changes, transitions and failed current readiness preserve pending.

3. **Same-manager lost-reply fence: PASS within the documented systemd 255 contract.** `personal_activate_manager.py:191–225, 282–323` checks the ordinary same-UID manager, fixed local bus, current manager process and supported version; recovery requires the saved bus/owner, explicit old-sender `NameHasNoOwner`, a matching Ping to that saved unique owner, then identity revalidation. Ordinary lookup errors do not establish disappearance. `personal-activate.py:426–435` still requires no queued unit job and either strict terminal state or fresh authenticated readiness after the fence. The source-based argument is limited to this synchronous same-UID v255 authorization path, not generic asynchronous polkit.

4. **Final dependency-boundary correction independently checked: PASS.** `personal_activate_manager.py:430–443` now refuses `.device` and `.swap` nodes before accepting a graph that cannot certify their hidden following sets. `test/test_personal_activate_manager.py:287–304` includes the canonical device with empty forward Following whose sibling can pull Worker, and a swap rejection. The existing recursive start/stop/requisite closure, cached command/property checks, stable before/after observations, already-active database requirement and no-worker/no-target scope remain enforced. This reviewer did not originate the device finding and does not present the other reviewer's work as its own.

5. **Historical receipt readiness semantics, reviewer finding resolved: PASS.** The preliminary implementation reported false for cleanup of a historical terminal receipt despite not checking the current application. `personal-activate.py:310, 331–343` now distinguishes a newly observed result from historical cleanup, returning `application_ready: "not_checked"` for every already-committed receipt. `test/test_personal_activate.py:393–425` interrupts terminal recovery after its receipt, changes runtime externally, forbids new application observations, and verifies historical cleanup neither claims current readiness nor changes the receipt. Fresh terminal observations still return false; fresh authenticated ready returns true. The documentation now matches this behavior.

6. **Lost-wire-reply versus missing durable ack, reviewer evidence concern resolved in the harness: PASS code inspection; actual execution remains a CI gate.** `test/personal-activate-crash.py:67–91` adds a separate real fixed-owner StartUnit message with auto-start/interactive authorization disabled, sends and flushes it, then exits 73 without waiting for or validating the reply. Production intent/attempt durability and all other manager operations remain in use. `test/personal-activate-integration.mjs:149–179, 248–287` distinguishes this case from `after_start_before_ack`, bounds observation-only recovery attempts, and accepts either a freshly proven ready invocation or a fenced terminal result without pretending the departing sender's request was accepted. The older checkpoint remains explicitly identified as a received reply whose acknowledgement was not durable. The suite is wired into the actual ordinary-user workflow and contains 18 activation checks; it is not a local root systemd simulation.

7. **Public CLI and scope reporting: PASS.** `src/cli.mjs:38–44` uses the fixed distribution interpreter with `-I -B` for Linux activation. `personal-activate.py:444–495` requires the ordinary Linux identity, rejects unknown/duplicate/abbreviated/action-inappropriate options, closes its connection, and prints bounded fixed error codes rather than supplied secrets or raw manager errors. Public status remains file-only and never represents historical receipt data as fresh readiness. No public injection hook or environment override was added for the internal test seams.

## Independent primary-source checks

The reviewer independently fetched the pinned source via the GitHub connector after GitHub blob-page web opens returned DisabledError. In systemd commit `db11bab38ccf1ed257f310d29070843d4c58ea01`, [dbus-unit.c](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/dbus-unit.c) lines 408–421 and 1825–1842 lead from authorization to synchronous job insertion. [bus-polkit.c](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/shared/bus-polkit.c) lines 502–506 return immediately on established privilege, and [bus-convenience.c](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/libsystemd/sd-bus/bus-convenience.c) lines 704–747 establish the ordinary same-UID path. [sd-bus.c](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/libsystemd/sd-bus/sd-bus.c) lines 2413–2515 and 2968–3049 show matching synchronous replies and normal queued message dispatch; this check did not find a Ping bypass through a deferred same-UID authorization. The [D-Bus connection documentation](https://dbus.freedesktop.org/doc/api/html/group__DBusConnection.html) describes queued dispatch and the final local disconnection notification; the fetched documentation identified itself as 1.16.2, not the exact Ubuntu daemon build. [service.c](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/src/core/service.c) lines 1933–2011 distinguishes before-auto-restart states from strict terminal states checked by the adapter.

## Tests actually executed on the exact final commit

| Reviewer-run command | Actual result | Saved local output |
| --- | --- | --- |
| `python3 -B -m unittest discover -s test -p 'test_personal_activate*.py' -v` | 101 passed: 52 state/CLI fixture tests and 49 manager tests | `recovery-9e85892-activation-python.log` |
| `python3 -B -m unittest discover -s test -p 'test_personal_ready*.py' -v` | 71 passed | `recovery-9e85892-ready-python.log` |
| `python3 -B -m unittest discover -s test -p 'test_personal_deploy*.py' -v` | 87 passed | `recovery-9e85892-deploy-python.log` |
| `node --test test/personal-activate-cli.test.mjs test/personal-ready-cli.test.mjs` | 10 passed | `recovery-9e85892-cli.log` |
| `node --test test/personal-deploy-cli.test.mjs` | 4 passed | `recovery-9e85892-deploy-cli.log` |
| `git diff --check 5b3c88826984a9b5c8b34ca2cf7ffff171501697 HEAD` | Passed, no output | Tool execution record |

Total: **259 Python tests and 14 Node CLI tests passed**, with zero failures in this reviewer's final runs. The reviewer also ran 256 Python and 6 CLI preliminary tests before the final corrections, all passing. These are distinct executions and are not combined to inflate the final test count. Implementation-agent first failures, including its initially incorrect missing-lock expectation, remain separate historical evidence and must not be erased by these reruns.

## CI and practical limits

This reviewer independently fetched the candidate's workflow collection. At that observation, `Personal user services` [PR run 35360845999](https://github.com/youq616/ultrabrain/actions/runs/35360845999) and [push run 35360841927](https://github.com/youq616/ultrabrain/actions/runs/35360841927) were still in progress. The [push portability run 35360841868](https://github.com/youq616/ultrabrain/actions/runs/35360841868) reported failure, while the [same-SHA PR portability run 35360845900](https://github.com/youq616/ultrabrain/actions/runs/35360845900) reported success; root was notified to preserve and investigate the first failure. This report does not infer its cause and does not certify the full CI gate as green.

All tests personally executed by this reviewer ran in the local root container. Manager/process/HTTP behavior uses explicit fixtures, with actual private filesystem/lock/HMAC operations and read-only proc checks where specified. The real systemd, PostgreSQL, authenticated HTTP and process-death integration must pass in the authorized disposable ordinary-user CI account before phase acceptance or merge. The code-review PASS above is for the exact reviewed commit only; any later application change requires review of the new full SHA.

Supported activation remains console-only, with PostgreSQL already active, a valid existing token and a cooperative account maintenance window. Cross-manager/reboot recovery, running updates, automatic stops/rollback, socket ownership, complete cached-setting attestation, source-content freezing, user-host deployment, Windows clients and real-model quality are outside this proof. These limitations are documented and were not weakened to obtain passing fixture results.
