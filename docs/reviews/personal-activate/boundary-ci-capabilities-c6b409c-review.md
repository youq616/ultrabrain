# Independent narrow CI capability setup review

Reviewed commit: **`c6b409c9864f0d0d97577bde9b2a3d84207ee717`**.

Reviewed tree: **`6921e7e0cc63ce915012d6736579f4b239210d14`**.

Actual reviewer: `/root/activate_boundary_review/dbus_abi_crosscheck`.

Verdict: **No additional concrete safety blocker found in the new workflow and documentation delta. The candidate is not approved as a working integration.** During this review the parent independently reported that both real service CI runs failed during setup, after capability configuration and manager restart, at the production GetNameOwner call with manager_unavailable/DBusException; zero acceptance checks ran. I have not independently read those CI logs or established the failure's cause. This narrow review does not override the whole-phase BLOCK or claim a startup timing explanation.

I read the exact candidate workflow and documentation, the existing diagnostic helper's fixed output and production invocation, and the production process/manager identity postconditions. The application paths `scripts`, `src`, and `package.json` compare unchanged against `def34fb9577167bebc6f7d41d0c6bb8138220087`. The worktree was clean and at the reviewed SHA/tree at initial inspection. Subsequent isolated checks extracted their code with `git show` at the full candidate SHA to avoid depending on concurrent worktree changes.

Concrete reviewed boundaries:

- `.github/workflows/personal-services.yml:70–88`: Linux ordinary real/effective UID, explicit CI/write/systemd flags, passwd HOME and environment HOME both fixed to `/home/runner`, and the exact synthetic installation path are required before mutations. The bounded cgroup read checks every hierarchy and rejects an exact `user@UID.service` path component. This checks the hosted runner's visible cgroup ancestry; it is not a general proof against a separately namespaced or privileged actor changing execution context. This workflow declares a hosted ubuntu-24.04 job, with no container context introduced in the reviewed step.
- `.github/workflows/personal-services.yml:91–124`: the root helper accepts only one canonical positive decimal UID, binds it to SUDO_UID and the account's fixed passwd home, and walks `/run/systemd/system/user@UID.service.d` through directory descriptors. Every component is opened with O_DIRECTORY/O_NOFOLLOW and checked for root ownership and absence of group/other write. New directories remain fixed children of those descriptors. The fixed drop-in filename is created with O_EXCL/O_NOFOLLOW, verified as a singly linked root-owned regular file, and receives only the literal two capability resets. Existing files or symlinks fail rather than being overwritten. File and parent-directory fsync are present. The step creates no user-selected root path.
- `.github/workflows/personal-services.yml:125–139`: system daemon-reload, linger, and restart commands have explicit timeout/kill bounds. Only the derived current runner's `user@UID.service` is restarted. The two loaded capability settings must both be empty before restart; a later conflicting drop-in therefore causes refusal. Fixed runtime/bus values are propagated for the same UID. The current-job cgroup guard precedes these changes.
- `.github/workflows/personal-services.yml:140–151` and `test/personal-activate-crash.py:246–260`: success requires the real diagnostic child to return zero with `ok is True`, which follows production LocalManager.connection_identity including the existing executable, UID, PID/start-time, namespace and manager identity checks. Actual proc status must additionally report manager permitted capabilities as zero; unavailable or nonempty capability observations fail. These are required postconditions, not postconditions achieved by the currently failing CI run. No fallback identity probe replaces the production checks.
- Output remains bounded and fixed in meaning: the diagnostic child uses existing allowlisted exception codes/types/frames and boolean/errno/label-enum proc observations, rather than exception messages, raw process status, arbitrary labels or service journals. The wrapper captures child stderr, validates the outer JSON shape and size, then emits that diagnostic. Fixed UID/runtime paths are the only new environment output. No new token, database URI or transcript output was identified.
- `docs/PERSONAL-ACTIVATE.md:14,77`: documents that matching UID alone is insufficient for proc access, and confines capability adjustment/restart to preparation of the disposable CI manager. It adds no user-host reset command or production permission workaround and does not claim real integration acceptance.

Primary systemd check: [systemd v255 systemd.exec.xml, lines 754–809](https://github.com/systemd/systemd/blob/db11bab38ccf1ed257f310d29070843d4c58ea01/man/systemd.exec.xml#L754) confirms that an empty CapabilityBoundingSet resets that set and affects effective/permitted/inheritable sets; empty AmbientCapabilities resets the ambient set. The documentation also describes command-prefix exceptions, reinforcing the value of checking actual manager CapPrm after startup rather than relying solely on parsed configuration. This source check is independent of implementation claims.

Executed evidence:

- **25 isolated checks passed**, using Python code extracted from the exact candidate workflow and fake OS/subprocess modules. No shell workflow was executed. Eighteen preflight cases covered an outside runner, same/hybrid target-manager cgroup rejection, other-manager and component-lookalike handling, empty/oversized/malformed/relative/NUL cgroup input, root and unequal IDs, all three CI flags, and environment/passwd/fixture home mismatch. Seven postcondition cases covered valid identity plus empty CapPrm, nonempty or unknown CapPrm, failed identity, nonzero child result, wrong diagnostic kind, and unexpected outer keys. These check branch behavior; they do not execute kernel cgroup, root filesystem, systemd or D-Bus operations.
- Exact candidate whitespace diff check passed. Exact application-path comparison against def34fb passed. Exact candidate tree was reconfirmed.

Not executed by this reviewer: real sudo, root drop-in writes, daemon reload, linger, manager restart, dependency installation, D-Bus/service integration, or the application's full suites. The main independent reviewer owns the 259 Python/14 CLI execution and full-phase verdict. No repository implementation was edited. Real CI setup failure remains unresolved by this narrow review.
