# 本地快照核验与差异对照：模块验收修正

基线 caf605ae591dea5b58b9322113d7781806d149bd，完整树 b0f1cffb277a757b1c71b6df41a5f854887117cd。当前仓库已包含快照导出及本地核验模块，不能用旧677阶段补丁覆盖。实际下载源码工件10633238591，ZIP SHA256为3b17dcd6df492d8cb17eec49536f78fd0cee27d0d33c210102f4157c62dcc03f；恢复全部跟踪文件、执行位及四个gitlink，重建树完全一致。继续原PR20草稿分支，不修改main或用户服务。

## 独立发现与产品修复

真正独立Codex review5265555599/comment4061276373对caf605提出P2：derivation中的1e400解码为Infinity，随后JSON.stringify会将其变成null，可能使内部指纹校验和差异对照把它与真正的null混淆。旧审查有具体发现，不是“未发现重大问题”的批准。

共享verifyMemorySnapshot现在在整份JSON的序列化预算检查中用replacer检查每一个数值，包括嵌套对象、数组、空键和__proto__数据键；非有限数值立即抛出memory_snapshot_unconfirmed。检查先于任何注入的哈希调用，也先于本地核验句柄生成，不让异常值静默变成null。合法有限数值的格式和哈希算法保持不变，字符串与null仍可正常核验和区分。这里是解码后的JSON数值比较，不是任意精度十进制或数字原始拼写比较，详见用户合同。

这是本轮唯一生产代码修改；不改变app.js、两个UI脚本、HTML、服务端数据操作、数据库迁移、MCP名称、权限、模型开关或上游锁。

## 两处完整CI阻断

caf605共有6组工作流成功、2组失败，不计完整验收。实际读取两个失败作业完整日志，选定原行保存在ci-first-failures.log（明确为摘录）。

预览run35588946647/job106298705820在File.prototype.arrayBuffer恢复表达式返回原生函数后失败，Playwright把该函数作为求值函数调用，缺少Blob接收者而报Illegal invocation。改为不返回函数的明确回调；保留原来的取消、重新核验及授权断言，并继续执行后续场景，不删除失败用例或提高时限。

Validate run35588946746/job106299121619在旧管理台断言失败：全页.memory-content选中了1个记忆结果和3个新增核验面板pre。把原来4处断言都限定到#results .memory-content，继续要求该范围内唯一目标及原文完全一致，不使用first()掩盖多条结果。后续实际n8n步骤在旧失败运行中被跳过，不能声称当时已通过。

## 回归及实现者自审

新增19项定向回归，包含直接Infinity/-Infinity/NaN、带null等价摘要的原始溢出JSON、嵌套/数组/转义键/原型名称键、有限数值极限、文字与null区分、任一文件失败后整组结果不可用，以及两个浏览器夹具修正的回归约束。在独立恢复的未修改caf605源码上重新执行，5通过/14失败，完整日志压缩入库；修正后的核验专项共80项通过。14是边界失败数，不是14个独立漏洞。

开发时两个新静态测试曾缺少readFileSync导入，初始探索输出包含该夹具错误；修复导入并加强“先检查结果被拒绝、再点击导出”的UI断言之后，才取得上述可靠的基线复现。早期输出随交付包保留，不归因于产品或独立reviewer。

普通账号oai、umask022、Node22.16.0全量1481/1481，0失败/取消/跳过，完整退出0；Python3.13.5全量683/683，测试报告57.958秒、监督过程59.817秒，退出0。本轮增加19项Node，80项核验专项是全量子集；Python单测未变。语法/AST/diff检查通过。一次本地Playwright冒烟尝试因未安装浏览器可执行文件在launch阶段停止，没有执行断言，不计浏览器或数据库通过；真实验收使用现有隔离CI。

原核验14项浏览器流程保留，追加3项：由Python独立计算null派生数据的摘要，先验证合法null快照，再分别让左侧/右侧使用拥有相同摘要的正/负溢出数值文件，必须整组拒绝、没有详情/差异/报告。新阶段定义17项；原文/差异分页、真实File读取取消、移动布局、无数据HTTP和六张表不变均须重新在最终SHA执行。准备数据有明确合成写入，不声称全程只读。

本记录创建时，最终提交CI及新独立复审仍PENDING。既有测试套件、CI阶段、超时和所有者权限均未缩减。模块范围是选择文件→本地核验→单独对照→分页差异→明确详情/无正文报告，不导入或恢复数据库，不调用模型。最终验收结论须绑定新SHA并补记PR20，不把历史失败改写成成功，不宣称零缺陷、用户部署或整个个人V1已发布。
