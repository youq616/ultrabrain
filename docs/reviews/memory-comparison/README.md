# 人工版本对照阶段：实现与自审记录

基线：`33c88d08c10f8a529bbd2693aa5cd0e197e04c23`，完整树 `454841172f38fc51e8c023139c1b89db61182e1e`。从已交付固定快照和增量恢复，补回原有已跟踪日志及四个 vendor gitlinks 后，整树匹配。延续草稿 PR #20；不修改 main、其他项目、生产服务、后端授权、MCP 目录、迁移或上游锁。

## 已实现和自审

管理台增加三方纯文本对照和独立的本地基准采用动作。`web/personal/app.js` 的 compareCurrentMemory 复用既有 exact-read 响应验证；采用函数在确认提示前后再次验证冻结会话、来源、版本、草稿和视图。读取与采用都不写库，写入仍走原事件/授权/expected_revision CAS。memoryDraft 把草稿可信度与已读取基准分开，避免采用新基准时静默改变未编辑的可信度。全部可编辑元数据都展示，不只展示正文。

实现助手另行检查取消、锁定、迟到响应、绕过DOM事件改变条件、二次并发、同版本不一致、最大版本、共享/文件片段、归档与未知交付的区别。补充5项自审回归：同版元数据冲突、最大版本、草稿可信度变化、确认提示期间撤回对照同意、真实模拟未知写入不得绕过原事件。未发现该检查范围内剩余阻断项；这是实现助手自审，不是独立代理批准。

## 已执行本地验证

普通账号 Node v22.16.0 全量 **825/825**，0失败/取消/跳过；Python3.13.5 全量 **683/683**，退出码0。新对照专项 **44/44** 是全量子集；其中最初39项功能测试加5项自审回归。本阶段没有修改Python单测。Node语法、浏览器Python AST与增量diff检查通过。

新增功能前，最初39项专项测试 **0通过/39失败**（缺少入口/函数）；完整原始输出保留于 first-failure.log.gz。这不是39个安全漏洞。首轮实现38/39通过，剩余失败是测试直接构造的pending缺少生产状态必有的input；修正夹具形状后通过，未放宽生产验证。两份失败日志与最终输出的哈希见local-evidence.json。

首次串联的Node全量已完成820/820，但随后Python受外层200秒执行时限中断，没有最终结论，不计通过；随后单独监督完整重跑Python，683项全部通过。新增自审用例后Node再次完整重跑到825。中断日志和最终日志分别保留，不把局部输出当全量成功。

## 真实集成与独立审查：创建记录时 PENDING

既有 Chromium/PostgreSQL 夹具保留19项只读预览并比较5张表，后续明确纠错阶段扩展人工对照、采纳前后记录不变、再次并发导致旧保存被拒绝、手工合并后保存、可信度保留、取消迟到查询及窄屏布局。最终数据库断言为revision8/candidate/LOOKUP_MANUAL_MERGE/confidence0.4。只有实际CI执行后才能记通过，不能从单测推断真实浏览器成功。该纠错阶段有明确合成写入，不冒称全程只读；不使用用户内容或真实模型。

全量CI和Windows客户端矩阵保留，新的JS专项已接入Linux/Windows任务。最终候选还须接受独立Codex审查；旧33c88d0结论不覆盖本次改动。实际reviewer未公开的内部会话、测试执行和逐文件覆盖不得编造。最终SHA、工作流身份和结论在PR评论补记，创建时PENDING不是批准。不合入未验证应用，不宣称零缺陷、整个个人V1发布或用户部署完成。
