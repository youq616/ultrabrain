# 客户端引用来源核对：阶段记录

基线 b6a6e84f4ab510f76f54da6caf3c505ad11914b6，完整树 bfee9e63263418f3f417f612d23f5e597940926c。从已验收CI源码归档恢复648份普通跟踪文件及4个gitlink，重建树一致；继续PR20原开发分支，不覆盖用户改动、不合入main、不改运行服务。

## 模块与边界

新增手动客户端lineage命令与可安装dist/lineage.cjs入口，将已有管理台直接来源核对提供给命令行/自研Agent。仅明确请求及同意时读取，默认输出元数据，include_text:true才输出正文/引文/直接来源全文。要求已有身份固定值与workspace；每次读取前身份复核，最多3次ID-only ultra_memory_read，最终重读根记录，复用原九字段引用语义。无新增MCP名称、写权限、自动Hook、模型、数据库模式或服务变更。合同详见../../CLIENT-LINEAGE.md。

目标及来源只允许全局/配置项目，检查在响应后执行，不声称服务端从未收到原ID。报告不是真实性认证或原子快照，默认元数据仍私有。传入请求冻结复制，记录严格核验并深度冻结；授权撤回、signal取消、身份变化均阻止后续读和交付。CLI绑定最初Profile，不在途中采用替换配置；库入口在异步清理后再次检查授权。

## 实现者自审与首败

自审发现两个新实现缺口：库入口异步close期间授权撤回仍可交付结果；握手异常可能传递原始错误文字。两个具名回归在修正前均失败，完整输出保存在self-audit-first-failure.log，修正后通过。这是实现者自审，不是独立代理发现。CLI在最后输出授权失败时保守记录读取已发生，不能误报未开始。

最初模块不存在时仅1个测试文件以ERR_MODULE_NOT_FOUND失败，不是59个漏洞。首次CLI测试9项失败来自合成SDK夹具缺少proxy静态导入的server模块，已在测试专用loader中补齐不可实例化的桩；没有改产品逻辑来掩盖，原始探索输出随交付保留。

## 实际本地结果

普通uid1000、Node22.16.0全量1766/1766，0失败/跳过/取消，退出0。Python3.13.5全量683/683，41.545秒，退出0。新增86项=59合同+18实际runtime/SDK-double+9实际CLI子进程/SDK-double；86专项是全量子集。原旧套件全部保留。语法、AST、YAML和diff检查通过。

本地没有Bun及完整已安装MCP SDK，未计本地打包/真实PostgreSQL/实际客户端协议通过。新增隔离CI阶段必须使用本提交实际构建、npm安装后的CLI和library，官方SDK、stdio/只读HTTP、四次合成生成器准备的真实数据，执行六表只读指纹与另一个只增加1事件的并发修改检查。四次生成器不是外部模型；所有旧工作流/阶段/时限不缩减。

本记录创建时当前最终提交CI及独立Codex审核仍PENDING。必须取得绑定新SHA的具体结果后补记PR20。旧b6绿灯及独立无重大问题结论不能覆盖新模块；不将本地SDK doubles当作官方宿主、真实模型回合或用户部署验收。最终无重大阻断不等于零缺陷保证。
