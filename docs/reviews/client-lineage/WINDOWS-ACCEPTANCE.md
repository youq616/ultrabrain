# 客户端模块验收：Windows测试启动修正

候选35a381a64a492adc9ea6f07ccd7cd71d7ded1bde（树1746259ce2740abbd2d54c8632b788b4e5716604）已得到独立结果5770977497的无重大问题结论，但Windows CI失败，因此尚不能完成验收。

## 实际失败与修正

run35684457913/job106608206411中，新增CLI子进程测试把磁盘路径传给Node的--import，Windows将D:识别为不支持的ESM协议，子进程未返回JSON。该作业715项中708通过、7失败；不是七项独立产品漏洞。完整日志实际已读，原选定行保存在ci-windows-first-failure.log，明确为摘录。

将测试预加载参数保持为new URL(...).href，而CLI脚本路径仍使用fileURLToPath。补充一个跨平台URL协议及路径往返约束，先在旧夹具上复现1失败，再通过；原日志保存在windows-url-first-failure.log。实际子进程仍会运行原全部行为断言，空stdout时先给出清晰的测试错误，不能把失败误报成通过。没有修改产品代码、测试覆盖列表或超时，也没有跳过Windows。

重新核对完整安装集成日志确认run35684457998/job106608206796实际成功：真实构建和npm安装后的CLI与library、官方SDK、PostgreSQL、stdio/只读HTTP，共20项通过。初次进度判断曾将该组误报为失败，已更正；最终仍需在修正提交上重新执行。35候选最终7组成功/1组Windows失败，不是全绿。

## 本地执行与边界

新增URL约束之后，模块87项全通过。本地一次全量Python以环境默认umask0002启动，旧ready夹具创建的中间目录可组写，被原安全检查拒绝，683项出现10个setup错误；原日志保留，未改安全校验。以明确umask0022及普通uid1000重新完整执行：Node1767/1767，零失败/跳过/取消，退出0；Python683/683，43.690秒，退出0。新累计87项=59合同+18runtime/SDK-double+10CLI/SDK-double，87专项属于全量子集。原始35候选日志与哈希未覆盖，环境失败不计通过。

这些本地CLI测试运行真实Node子进程但协议为SDK doubles，不冒充实际网络或用户Agent。真实安装式验证来自隔离CI。既有全部Windows/Linux、浏览器、n8n及历史升级阶段继续保留；main、用户服务、上游锁、权限与模型配置未改。

本记录创建时修正提交的CI及独立复审仍待结果。35的批准不覆盖之后的测试变化；最终验收必须绑定新SHA并补记PR20。源码中的历史PENDING和计数按相应提交理解，不将其当成最终结论。
