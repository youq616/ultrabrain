**Independent review verdict: BLOCK for `fde82fa97843d22b3f1a92fb83926cc99d53dcaa`.**

Reviewer session: `/root/ready_binding_review`. Reviewed tree: `23553b76ec2fab50268c7217fa6ba3c8aa64cd59`. Comparison base: `d2d8c30b939000b26fe41fc0e5bcab618232ca28`. I did not implement this phase. I read `AGENTS.md`, inspected the changed implementation and affected deployment, filesystem, runtime, and CI boundaries, and ran independent tests. No repository files, services, credentials, or databases were changed by this review; additional probes used a scratch export of the exact reviewed commit.

The blocking finding is:

- **P1 — A complete valid HTTP response can time out because the client requires connection EOF.** In `scripts/personal-ready.py:180–191`, the receive loop continues until `recv()` returns empty and only then calls `parse_http()`. A response already containing the full declared `Content-Length` is therefore insufficient. A server retaining the socket causes `readiness_timeout` despite a complete response. I independently reproduced this using an actual loopback socket: the server sent a valid complete length-delimited response and retained the connection; the client timed out at the injected 150 ms deadline. This corroborates the first actual PR CI failure, which I fetched independently: [Personal user services run 35291857487](https://github.com/youq616/ultrabrain/actions/runs/35291857487), job `105436203942`, reported `owned_live_console`, `readiness_timeout`, and `personal_readiness_failure_after_checks:0`. The correction should recognize completion from strictly validated framing while retaining the existing duplicate-header, transfer-encoding, content-encoding, length, header-size, and total-byte rejection rules. The corrected commit needs new independent approval and successful actual services CI.

Other reviewed boundaries were acceptable within the stated scope:

- **Installed configuration and cooperative lock:** `scripts/personal-ready.py:248–290, 298–320` holds the existing account-global shared deployment lock, refuses pending deployment work, compares the caller’s accepted receipt, reconstructs the console-only plan, and rechecks observed identities. Reused `scripts/personal-deploy.py:226–253` validates receipt contents and the complete export inventory and bytes. This is materially stronger than checking only `FragmentPath`. Existing strict private-file and directory checks were not relaxed.
- **Console process binding:** `scripts/personal_ready_process.py:194–246` checks the actual executable inode, complete NUL-terminated argv, PID/start ticks, four UID values, unified cgroup, and PID/network namespaces. `scripts/personal-ready.py:306–319` compares the authenticated response’s PID and invocation with the manager observation and rechecks the process afterward. No process environment is read. HMAC authentication is correctly distinguished from socket-owner verification.
- **Physical database binding:** `scripts/personal_ready_process.py:270–321` validates managed configuration and pinned runtime metadata, reads the private complete PID file, verifies the selected postmaster’s executable and actual PGDATA working-directory inode, and requires the returned backend to be its real child with the same executable and relevant namespaces. Rechecks around the exchange cover observable replacements. A UUID alone is not used to infer physical installation ownership.
- **Read-only SQL boundary:** `src/personal-readiness.mjs:42–65` executes fixed SQL after authentication, uses a read-only transaction and local timeouts, and does not read memory bodies or call migrations, health, model, or connect. I independently inspected the pinned upstream [PostgresEngine transaction implementation](https://github.com/youq616/gbrain/blob/a6be012a3bcfac42e279630aedec5cda4a450e29/src/core/postgres-engine.ts): lines 539–555 bind the callback’s engine to one transaction connection, so these timeout and transaction statements do not silently run on unrelated pooled connections.
- **PID-file timestamp distinction:** The absence of a false exact comparison between SQL `pg_postmaster_start_time()` and the PID-file timestamp is correct. I independently checked the pinned PostgreSQL sources: `PostmasterMain` calls `ChangeToDataDir()` before creating the data-directory lock file, and records `PgStartTime` later; the PID-file writer uses `MyStartTime`. Sources: [postmaster initialization](https://github.com/postgres/postgres/blob/724edf9bde9d356724ad384a2e196edc3c9f80f7/src/backend/postmaster/postmaster.c), [PID-file writer](https://github.com/postgres/postgres/blob/724edf9bde9d356724ad384a2e196edc3c9f80f7/src/backend/utils/init/miscinit.c).
- **Actual integration fixture:** `test/personal-ready-integration.mjs:37–47` restricts execution to the authorized disposable ordinary-user CI installation and an already-owned console-only fixture. Token rotation is restored in `finally`; the source-removal and table-lock scenarios restore their state; lock cleanup retains its own failures. The surrounding deployment fixture remains responsible for owned-service cleanup. The final snapshot compares configuration, deployment state, service invocation, enablement-related state, token bytes, and record counts. These checks do not constitute a claim of byte-for-byte preservation of every PostgreSQL row.

One nonblocking claim-scope limitation should remain explicit in the corrected implementation and acceptance report: `scripts/personal-ready.py:267–284` observes selected manager properties and the actual console argv; it does **not** attest every cached systemd setting, such as cached `UMask`, `Restart`, or dependency configuration. Full installed bytes plus actual argv catch the current generator’s root/Bun/source/port differences, but should not be described as complete active-manager configuration attestation. This matters especially when older file timestamps can leave `NeedDaemonReload=no`.

Executed evidence:

| Command or inspection | Actual result |
|---|---|
| `python3 -I -B -m unittest discover -s test -p 'test_personal_ready*.py' -v` | **69 passed**, zero failures |
| `node --test test/personal-ready-cli.test.mjs test/personal-readiness.test.mjs` | **33 passed**, zero failures or skips |
| `python3 -I -B /workspace/scratch/be43dca0ab34/ready-binding-independent-fde.py` | **7 passed**: six independent rejection probes, plus a successful reproduction of the blocking EOF defect |
| Independently fetched PR services CI run `35291857487` / job `105436203942` | **Failed** on the first owned live-console readiness check |
| `git diff --check` at review completion | Passed; repository worktree remained clean at the reviewed SHA |

The six extra rejection probes covered a same-length unit modification with restored old mtime, an unexpected generation file, token modification and restoration with the original inode and mtime, replacement of the shared lock inode during probing, backend identity change at the last verification boundary, and a symlink to otherwise identical unit bytes. All were refused as expected.

This root container cannot provide the required ordinary-user systemd/PostgreSQL acceptance. The local passing tests are fixture, protocol, actual-loopback, and limited actual-procfs evidence. They do not override the actual CI failure. Approval of this commit is withheld; the corrected exact SHA requires follow-up review.
