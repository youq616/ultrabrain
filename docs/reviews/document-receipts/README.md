# 文档写入回执：实现自审及验证记录

基线 `9cbd37963006948d72a8794d884327791a29b3af`，完整树 `a0f55b7f37e13a9e62a148a99333bf6b8d31dbe3`。本地从固定源码快照与已交付补丁重建，保留执行位、历史日志和四个 vendor gitlinks，重建树与上述基线完全一致。继续 PR #20 的原开发分支，不改 main、用户服务或 qbrain。

## 上阶段门槛补齐

本轮重新查询基线的8组工作流，全部 success。主验证35480045641完成于2026-09-20T01:19:23Z；实际读取n8n-engine105996698440完整日志，确认原管理台26项真实Chromium检查通过、随后27项n8n检查包含真实CLI执行。上一轮“排队/未执行”是当时观察，不是当前状态；PR评论5748935354已补记。它们不认证本次新代码。

## 本轮修复和首败

原 verifyWriteReceipt 跳过文档导入/排队/归档，所以错误原文指纹、标签、范围、文档ID、归档保留标志等只要通过通用HTTP封套就会清除pending。先运行实际页面JS的65项模拟回归：4通过、61失败；原始失败日志压缩为first-failure.log.gz。它是若干合同缺口的边界组合，不是61个独立漏洞。

现在按现有后端字段核对文档回执，并在点击排队/归档时单独冻结ID/字节数/版本，用于本地核对，不改变发送给服务器的JSON。异常回执保留原事件，明确重试不创建新事件，导入重试仍受原选择和同意约束。不会清空独立草稿，也不会把历史事件的回执伪装成最新文档状态。没有修改后端、SQL、迁移、MCP目录、模型权限或依赖锁。

## 实现助手自审

与src/personal-documents.mjs实际返回字段及personal-document-core.mjs的分片算法逐项核对；加入14项边界和竞态回归：最大128KiB、中间32KiB附近中文边界、BOM-only/NUL-only/空白文件可导入但未必可排队、非法短中间片段、三类写入期间会话改变，以及提交后文件选择器改为另一文件。最终文档专项79项。原记忆回执套件中唯一使用假search结果冒充归档回执的夹具改为现有归档合同，保留“不清空独立草稿”的原断言。

这些是实现助手的自审，不是独立代理。创建记录时，最终SHA的独立Codex复审和CI均PENDING；发布后必须读取实际结果，不能沿用基线结论。

## 本地实际运行

普通账号oai、Node22.16.0、Python3.13.5。首次65项修复后65通过；初次Node全量960/960。补充自审后最终Node974/974，0失败/取消/跳过，完整退出0；文档专项79/79是全量子集。Python完整683/683，40.764秒，退出0。一次45秒外层时限终止的Python尝试没有可靠退出码，日志单独保存，不计通过；随后有界独立进程完整重跑才计成功。页面JS和集成fixture语法、浏览器Python AST、增量diff检查通过。

当前容器的浏览器访问loopback被ERR_BLOCKED_BY_ADMINISTRATOR拦截。本地HTTP模拟演练没有到达任何文档操作，不能计作浏览器或数据库通过，不规避环境策略。真实Chromium/PostgreSQL验证使用仓库现有隔离CI。原始日志/退出码与源码blob摘要见local-evidence.json；它标记的是本地执行，不是远程CI结果。

## 真实集成验收门槛

原19项只读预览、11项人工纠错及10项记忆回执检查保持。追加文档阶段：真实import/queue/archive已执行后，只破坏回送浏览器的回执；明确重试原事件，检查授权撤回、去重、中文分片与哈希、原文精确下载，以及导入被并发归档后重放只确认历史事件。新阶段定义8项浏览器检查，9次浏览器写入传输/5个唯一事件，另有一次明确直接API归档，实际数据库增量必须是6事件、2文档、2片段与2个零尝试的失效任务；无关未保存草稿不入库。

CI成功不等于真实模型质量或用户部署。完整字段范围及不证明的事项见docs/PERSONAL-DOCUMENT-RECEIPTS.md；尤其排队指纹只有形状校验，不是本地原文预映像证明，任务取消/模型整理及document_read的扩展不在本轮范围。
