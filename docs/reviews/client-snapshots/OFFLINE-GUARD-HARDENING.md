# 离线快照只读验收加固：本地修正，独立复审待完成

基线提交 `094f24d0a5cc35f95596d93a90084573c7521540`，完整树
`a940081f25b4477592ac3e86729038715598cdfd`。本轮保留已有四操作、离线入口、
服务端、数据库、上游锁及用户服务，不扩大读取或写入许可。

## 外部发现与实际修正

PR #20 的独立审查评论 4068778927（review 5274517982，
`chatgpt-codex-connector[bot]`，2026-09-22T05:50:11Z）指出原
`test/fixtures/snapshot-offline-guard.cjs` 只拦截高层写入：
可写打开、文件描述符和 FileHandle 写入仍可穿过，验收不能支撑其只读断言。
该发现针对基线，不是对本轮修正的批准；审查者内部运行和实际测试清单未披露。

新增 58 项独立 Node 子进程防护测试。先对原守卫执行：58 项中 7 通过、
51 失败、退出 1；完整原始输出保留。这是验收缺口的反向测试，不是 51 个
独立产品漏洞。修正后 58 项通过。修改包括同步/回调/Promise 打开的只读
标志白名单、拒绝含 O_CREAT/O_TRUNC/O_APPEND 的数字标志、低层 write/writev、
描述符截断及 FileHandle 写入/元数据修改拦截；仅 FD 1/2 保留输出许可。

实现者另补充“异常被应用捕获”的检查：即使应用将异常转换为 JSON、重设
exitCode 或调用 exit(0)，被拦截操作也会留下非零退出状态。这样不能因
异常清洗而把禁止操作误计为成功。原始异常文字继续不进入产品输出。

真实只读描述符、stat/read/close、CLI stdout/stderr 与库正常调用保持可用。
这些补丁只用于测试，不是向生产客户端注入守卫。守卫不是敌对 JavaScript
沙箱，不覆盖原生扩展、process.binding、预加载前保存的函数引用、操作系统
网络文件系统或用户将 stdout/stderr 重定向到文件；读取可能更新 atime。

## 真实安装式入口与文件竞态

新增 `test/client-snapshot-offline-package.mjs`：核对构建 manifest 后，将
实际 `snapshot-cli.cjs` / `snapshot.cjs` 放到没有 SDK 的目录，在守卫下
执行 CLI、路径库 API、字节库 API 的 inspect/compare/page/record、默认
不披露正文、明确正文/指纹、同意拒绝、指纹不符和非法输入。25 项检查
包含 2 项入口摘要检查；它们不是 25 个真实模型回合。

本地实际运行的是 CI 工件 10678682120 中的基线构建包，源代码和安装包
指纹记录于外部证据 actual-package.json。不是本轮本机重新 Bun 构建或
npm 安装；没有使用 SDK doubles。输入为合成快照，文件字节与 mtime
前后保持，模型调用为零；不取代原有真实 PostgreSQL 导出阶段。

新增读取途中把父目录换成链接的竞态回归，要求拒绝交付且关闭描述符。
Windows 分支使用 junction，POSIX 使用目录符号链接，不跳过平台，不降低
断言。该回归在 Linux 执行通过；没有在本地 Windows 执行。先前远端
Windows 失败日志与随后读取的精确 SHA 测试文件存在用例名称/位置差异，
所以不以日志位置猜改代码，也不声称这个新增回归已使旧 Windows 作业通过。

## 执行与验收边界

本地普通 uid1000、umask0022、Node22.16.0 执行全量回归；最终数量与完整
日志存放于交付证据，不将专项重复计入总数。Python3.13.5 全部 683 项按
七个不重叠批次执行，0 失败/错误/跳过；保留完整发现清单。之前单次
全量运行被工具时限终止，其日志保留且不计通过。

原 Windows/Linux 矩阵、全部已有测试、集成步骤、权限和超时保持不变；
只在现有任务流追加守卫反向测试和无 SDK 安装式入口检查。本轮没有启动
用户服务、修改 main、调用外部模型或数据库。

当前会话没有可调用的 GitHub 写入及独立审查触发动作。因此修正仅在本地
重建的仓库中；未推送、未取得修正后的远端 CI、未取得修正后的独立代理
批准。自审、测试和另一目录都不等于独立代理审核。保持验收 PENDING，
不得把基线 CI、旧审查或本地测试计作最终提交已通过审核。
