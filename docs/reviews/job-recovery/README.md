# 执行回执与恢复模块：开发和自审

基线 bff36ca5da5f46f527a5b9704dcc004175af4277，完整树268338562a4d045bbd6bb2ebd076b07f8307bb17。已下载实际源码工件10608693130，其ZIP SHA256为ad97a5d9a041ce8abed5117d011f63f919d10496fe6000d2089ce82fcedb08b5；恢复源码、执行位及四个gitlink后重建同一树。未套用过时聊天补丁。

上一提交8组CI均成功，但最终独立复审5261072091/4057434213提出P1：consolidate跳过业务回执验证，HTTP成功的空对象可清除原任务请求和不确定重试保护。因此上一阶段不能宣称独立验收通过。本轮首先解决此实际意见，不将Completed活动或绿灯当作批准。

先在未修改的bff页面运行28项定向回归：8通过、20失败，完整首败压缩保存在first-failure.log.gz。它们是合同边界用例，不是20个独立漏洞。修正增加完整处理回执校验，并将完成候选回执谓词与任务元数据校验共用。之后实现只读状态核对、独立同意、再次读终态后结束本地追踪及重试前新读取/费用确认。最终新专项53/53，本地Node全量1301/1301，0失败/取消/跳过；完整日志保存在交付证据包，摘要指纹见local-evidence.json。

实现者自审重点：所有真实处理返回分支，包括无工作/needs_model/skip/lease_lost；先后两次确认的来源、任务、原文选择与会话一致；确认提示期间导航变化不能重新授权；完成于两次观察之间的任务不能再次调用；失败/被取消的观察不等于找回原回执；同意撤回、查询失败、取消和迟到响应继续保留原请求。保留普通写入的原事件语义，模型操作明确不声称exactly-once。

真实集成追加在既有任务模块之后，不删除任何旧阶段。已实际提交执行前，浏览器/DB结果和独立复审均PENDING，不沿用bff绿灯。集成必须验证真实服务已执行后损坏回执、实际先于服务器的断线、原JSON重试，以及四个明确注入的合成生成器调用；数据库须精确验证五条保留原文、两条正确关联的私有候选和每条任务次数、终态、清除租约。外部模型调用为零，不宣称模型质量。

本地一次Python运行被外层45秒等待中断，即使日志已经打印用例结果也没有可靠结束状态，不计最终通过；其输出单独保留。后续使用有界监督进程重跑并记录真实退出码。当前验证数以local-evidence.json及最终PR评论为准。

所有源码仅追加原开发分支，不改main、用户运行服务、qbrain、数据库迁移、MCP工具名称、依赖锁或模型权限。最终独立复审必须绑定修正版完整SHA；实现者自审、独立审查和CI执行证据分开记录，不编造reviewer内部会话或执行明细。
